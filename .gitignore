# Chromium build output / checkout artifacts (never committed to the overlay repo)
/out/
/src/
*.pyc
__pycache__/
.DS_Store
# distributor-provided Google API keys (see roamux/build/google_keys.gni.template)
/roamux/build/google_keys.gni

# Roamux (roam-32): vendored Sparkle — fetched by roamux/build/fetch_sparkle.py (pin+hash), never committed.
roamux/third_party/sparkle/*
!roamux/third_party/sparkle/README.md
