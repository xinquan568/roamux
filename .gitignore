# Chromium build output / checkout artifacts (never committed to the overlay repo)
/out/
/src/
*.pyc
__pycache__/
.DS_Store
# distributor-provided Google API keys (see roamex/build/google_keys.gni.template)
/roamex/build/google_keys.gni

# Roamex (roam-32): vendored Sparkle — fetched by roamex/build/fetch_sparkle.py (pin+hash), never committed.
roamex/third_party/sparkle/*
!roamex/third_party/sparkle/README.md
