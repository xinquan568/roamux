diff --git a/chrome/browser/ui/views/frame/browser_view.cc b/chrome/browser/ui/views/frame/browser_view.cc
index bcb1930a86..ed52033a50 100644
--- a/chrome/browser/ui/views/frame/browser_view.cc
+++ b/chrome/browser/ui/views/frame/browser_view.cc
@@ -152,6 +152,7 @@
 #include "chrome/browser/ui/views/frame/contents_separator.h"
 #include "chrome/browser/ui/views/frame/custom_floating_corner.h"
 #include "chrome/browser/ui/views/frame/horizontal_tab_strip_region_view.h"
+#include "roamex/browser/ui/tabs/tab_strip_placement_observer.h"
 #include "chrome/browser/ui/views/frame/layout/browser_view_layout.h"
 #include "chrome/browser/ui/views/frame/layout/browser_view_layout_delegate_impl.h"
 #include "chrome/browser/ui/views/frame/main_background_region_view.h"
@@ -1050,6 +1051,14 @@ BrowserView::BrowserView(Browser* browser)
                                 base::Unretained(this)));
   }
 
+  // Roamex (roam-7): live tab-strip placement switching.
+  roamex_tab_strip_placement_observer_ =
+      std::make_unique<roamex::tabs::TabStripPlacementObserver>(
+          browser_->profile()->GetPrefs(),
+          base::BindRepeating(
+              [](BrowserView* view) { view->InvalidateLayout(); },
+              base::Unretained(this)));
+
   GetViewAccessibility().SetRole(ax::mojom::Role::kClient);
 
   if (GetFocusManager()) {
diff --git a/chrome/browser/ui/views/frame/browser_view.h b/chrome/browser/ui/views/frame/browser_view.h
index bb0e0958c9..2aaf538953 100644
--- a/chrome/browser/ui/views/frame/browser_view.h
+++ b/chrome/browser/ui/views/frame/browser_view.h
@@ -97,6 +97,10 @@ class TopContainerView;
 class TopControlsSlideController;
 class TopControlsSlideControllerTest;
 class VerticalTabStripRegionView;
+
+namespace roamex::tabs {
+class TabStripPlacementObserver;
+}  // namespace roamex::tabs
 class WebAppFrameToolbarView;
 
 namespace gfx {
@@ -1261,6 +1265,11 @@ class BrowserView : public BrowserWindow,
   // The view responsible for housing the contents of the vertical tab strip.
   raw_ptr<VerticalTabStripRegionView> vertical_tab_strip_region_view_ = nullptr;
 
+  // Roamex (roam-7): relayouts this view on roamex.tabs.strip_position
+  // changes (live placement switch).
+  std::unique_ptr<roamex::tabs::TabStripPlacementObserver>
+      roamex_tab_strip_placement_observer_;
+
   // Outward-projecting corners of the vertical tab strip.
   raw_ptr<CustomFloatingCorner> vertical_tab_strip_top_corner_ = nullptr;
   raw_ptr<CustomFloatingCorner> vertical_tab_strip_bottom_corner_ = nullptr;
diff --git a/chrome/browser/ui/views/frame/layout/browser_view_tabbed_layout_impl.cc b/chrome/browser/ui/views/frame/layout/browser_view_tabbed_layout_impl.cc
index 8ed88c3e4b..3f3758fd03 100644
--- a/chrome/browser/ui/views/frame/layout/browser_view_tabbed_layout_impl.cc
+++ b/chrome/browser/ui/views/frame/layout/browser_view_tabbed_layout_impl.cc
@@ -22,7 +22,9 @@
 #include "chrome/browser/ui/views/bookmarks/bookmark_bar_view.h"
 #include "chrome/browser/ui/views/frame/custom_corners_background.h"
 #include "chrome/browser/ui/views/frame/custom_floating_corner.h"
+#include "chrome/browser/profiles/profile.h"
 #include "chrome/browser/ui/views/frame/horizontal_tab_strip_region_view.h"
+#include "roamex/common/tab_strip_placement.h"
 #include "chrome/browser/ui/views/frame/immersive_mode_controller.h"
 #include "chrome/browser/ui/views/frame/layout/browser_view_layout_delegate.h"
 #include "chrome/browser/ui/views/frame/layout/browser_view_layout_impl.h"
@@ -651,7 +653,24 @@ BrowserViewTabbedLayoutImpl::CalculateProposedLayout(
   if (IsParentedTo(views().horizontal_tab_strip_region_view,
                    views().browser_view)) {
     gfx::Rect tabstrip_bounds;
-    if (tab_strip_type == TabStripType::kHorizontal) {
+    // Roamex (roam-7): flag-gated bottom docking — carve the strip band off
+    // the bottom edge; the top container and contents lay out in the
+    // remainder. Only the normal horizontal mode is affected; upstream
+    // vertical tabs and immersive reparenting keep stock behavior.
+    const bool roamex_bottom_strip =
+        tab_strip_type == TabStripType::kHorizontal &&
+        roamex::GetTabStripPlacement(browser()->profile()->GetPrefs()) ==
+            roamex::TabStripPlacement::kBottom;
+    if (roamex_bottom_strip) {
+      const roamex::BottomStripLayout roamex_layout =
+          roamex::ComputeBottomStripLayout(
+              params.visual_client_area,
+              views()
+                  .horizontal_tab_strip_region_view->GetPreferredSize()
+                  .height());
+      tabstrip_bounds = roamex_layout.strip;
+      params.visual_client_area = roamex_layout.remaining;
+    } else if (tab_strip_type == TabStripType::kHorizontal) {
       const int leading_margin = GetHorizontalTabStripLeadingMargin(params);
       tabstrip_bounds = GetBoundsWithExclusion(
           params, views().horizontal_tab_strip_region_view, leading_margin);
diff --git a/chrome/browser/ui/BUILD.gn b/chrome/browser/ui/BUILD.gn
index a087bb5..caced0b 100644
--- a/chrome/browser/ui/BUILD.gn
+++ b/chrome/browser/ui/BUILD.gn
@@ -384,6 +384,7 @@ static_library("ui") {
     "//chrome/browser/profiles:profile_manager_impl",
     "//chrome/browser/profiles:profile_util",
     "//chrome/browser/profiles:profile_util_impl",
+    "//roamex/browser/ui/tabs",
     "//chrome/browser/profiling_host",
     "//chrome/browser/renderer_host",
     "//chrome/browser/resources",
