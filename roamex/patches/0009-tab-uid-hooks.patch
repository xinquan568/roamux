diff --git a/chrome/browser/profiles/chrome_browser_main_extra_parts_profiles.cc b/chrome/browser/profiles/chrome_browser_main_extra_parts_profiles.cc
index 1aab47b6d0..54f0767df2 100644
--- a/chrome/browser/profiles/chrome_browser_main_extra_parts_profiles.cc
+++ b/chrome/browser/profiles/chrome_browser_main_extra_parts_profiles.cc
@@ -203,6 +203,7 @@
 #include "chrome/browser/profile_resetter/triggered_profile_resetter_factory.h"
 #include "chrome/browser/profiles/batch_upload/batch_upload_service_factory.h"
 #include "chrome/browser/profiles/renderer_updater_factory.h"
+#include "roamex/browser/tabs/tab_uid_service_factory.h"
 #include "chrome/browser/push_messaging/push_messaging_service_factory.h"
 #include "chrome/browser/reading_list/reading_list_model_factory.h"
 #include "chrome/browser/reduce_accept_language/reduce_accept_language_factory.h"
@@ -648,6 +649,8 @@ ChromeBrowserMainExtraPartsProfiles::~ChromeBrowserMainExtraPartsProfiles() =
 // static
 void ChromeBrowserMainExtraPartsProfiles::
     EnsureBrowserContextKeyedServiceFactoriesBuilt() {
+  // Roamex (roam-10): durable tab uid registry.
+  roamex::tabs::TabUidServiceFactory::GetInstance();
   // ---------------------------------------------------------------------------
   // Redirect to those lists for factories that are part of those modules.
   // Module specific registration functions:
diff --git a/chrome/browser/ui/browser_live_tab_context.cc b/chrome/browser/ui/browser_live_tab_context.cc
index a669a623c4..1b33d16d64 100644
--- a/chrome/browser/ui/browser_live_tab_context.cc
+++ b/chrome/browser/ui/browser_live_tab_context.cc
@@ -19,6 +19,7 @@
 #include "chrome/browser/glic/glic_tab_restore_helper.h"
 #include "chrome/browser/performance_manager/public/background_tab_loading_policy.h"
 #include "chrome/browser/profiles/profile.h"
+#include "roamex/browser/tabs/tab_uid_tab_helper.h"
 #include "chrome/browser/sessions/session_service_utils.h"
 #include "chrome/browser/sessions/tab_restore_service_factory.h"
 #include "chrome/browser/tab_group_sync/tab_group_sync_service_factory.h"
@@ -157,6 +158,9 @@ std::map<std::string, std::string> BrowserLiveTabContext::GetExtraDataForTab(
 
   glic::PopulateGlicExtraData(tab_strip_model_->GetTabAtIndex(index),
                               &extra_data);
+  // Roamex (roam-10): the durable tab uid rides closed-tab entries too.
+  roamex::tabs::TabUidTabHelper::PopulateExtraData(
+      tab_strip_model_->GetTabAtIndex(index), &extra_data);
 
   return extra_data;
 }
diff --git a/chrome/browser/ui/browser_tabrestore.cc b/chrome/browser/ui/browser_tabrestore.cc
index 9291009f10..3ed1826ff7 100644
--- a/chrome/browser/ui/browser_tabrestore.cc
+++ b/chrome/browser/ui/browser_tabrestore.cc
@@ -13,6 +13,7 @@
 #include "chrome/browser/apps/app_service/web_contents_app_id_utils.h"
 #include "chrome/browser/glic/glic_tab_restore_helper.h"
 #include "chrome/browser/profiles/profile.h"
+#include "roamex/browser/tabs/tab_uid_tab_helper.h"
 #include "chrome/browser/sessions/session_restore.h"
 #include "chrome/browser/sessions/session_service_base.h"
 #include "chrome/browser/sessions/session_service_lookup.h"
@@ -85,6 +86,10 @@ std::unique_ptr<WebContents> CreateRestoredTab(
                                extension_app_id);
 
   glic::RestoreGlicStateFromExtraData(web_contents.get(), extra_data);
+  // Roamex (roam-10): stash the restored tab uid for adopt-or-restamp at
+  // tab attach.
+  roamex::tabs::TabUidTabHelper::SetPendingRestoredUid(web_contents.get(),
+                                                       extra_data);
 
   std::vector<std::unique_ptr<NavigationEntry>> entries =
       ContentSerializedNavigationBuilder::ToNavigationEntries(
diff --git a/chrome/browser/ui/tabs/tab_features.cc b/chrome/browser/ui/tabs/tab_features.cc
index 230fa95035..3623840564 100644
--- a/chrome/browser/ui/tabs/tab_features.cc
+++ b/chrome/browser/ui/tabs/tab_features.cc
@@ -23,6 +23,7 @@
 #include "chrome/browser/enterprise/data_protection/data_protection_navigation_controller.h"
 #include "chrome/browser/enterprise/reporting/saas_usage/saas_usage_navigation_observer.h"
 #include "chrome/browser/glic/host/context/glic_page_features_manager.h"
+#include "roamex/browser/tabs/tab_uid_tab_helper.h"
 #include "chrome/browser/glic/suggestions/contextual_cueing_helper.h"
 #include "chrome/browser/image_fetcher/image_fetcher_service_factory.h"
 #include "chrome/browser/indigo/indigo_page_action_controller.h"
@@ -176,6 +177,9 @@ void TabFeatures::Init(TabInterface& tab, Profile* profile) {
   tab_subscriptions_.push_back(
       tab.RegisterWillDiscardContents(base::BindRepeating(
           &TabFeatures::WillDiscardContents, weak_factory_.GetWeakPtr())));
+
+  // Roamex (roam-10): flag-gated durable tab uid.
+  roamex::tabs::TabUidTabHelper::MaybeCreateForTab(tab, profile);
   tab_subscriptions_.push_back(webui::InitEmbeddingContext(&tab));
 
   // TODO(crbug.com/346148554): Do not create a SidePanelRegistry or
@@ -624,6 +628,10 @@ void TabFeatures::WillDiscardContents(tabs::TabInterface* tab,
 
   Profile* profile = tab->GetBrowserWindowInterface()->GetProfile();
 
+  // Roamex (roam-10): a discard is the same live tab — carry the uid across.
+  roamex::tabs::TabUidTabHelper::WillDiscardContents(tab, old_contents,
+                                                     new_contents);
+
   // Deregister side-panel entries that are web-contents scoped rather than tab
   // scoped.
   side_panel_registry_->Deregister(
diff --git a/chrome/browser/ui/tabs/BUILD.gn b/chrome/browser/ui/tabs/BUILD.gn
index e5deeb3..e132ab8 100644
--- a/chrome/browser/ui/tabs/BUILD.gn
+++ b/chrome/browser/ui/tabs/BUILD.gn
@@ -607,6 +607,18 @@ if (!is_android) {
     if (is_chromeos) {
       deps += [ "//chrome/browser/apps/app_service" ]
     }
+
+    # Roamex (roam-10): the chrome-facing uid helper/factory are roamex-owned
+    # files compiled into this target (they need Profile/SessionService; a
+    # separate roamex target would create a GN cycle). The pure registry lives
+    # in //roamex/browser/tabs.
+    sources += [
+      "//roamex/browser/tabs/tab_uid_service_factory.cc",
+      "//roamex/browser/tabs/tab_uid_service_factory.h",
+      "//roamex/browser/tabs/tab_uid_tab_helper.cc",
+      "//roamex/browser/tabs/tab_uid_tab_helper.h",
+    ]
+    deps += [ "//roamex/browser/tabs" ]
   }
 
   # This is a separate component to avoid possible circular deps.
