diff --git a/chrome/browser/ui/browser_commands.cc b/chrome/browser/ui/browser_commands.cc
index 6f7b8dd8fa..1b9230d677 100644
--- a/chrome/browser/ui/browser_commands.cc
+++ b/chrome/browser/ui/browser_commands.cc
@@ -67,6 +67,7 @@
 #include "chrome/browser/ui/bookmarks/bookmark_utils_desktop.h"
 #include "chrome/browser/ui/browser.h"
 #include "chrome/browser/ui/browser_command_controller.h"
+#include "roamex/browser/tabs/tab_initial_url_helper.h"
 #include "chrome/browser/ui/browser_finder.h"
 #include "chrome/browser/ui/browser_live_tab_context.h"
 #include "chrome/browser/ui/browser_tabstrip.h"
@@ -370,6 +371,11 @@ content::WebContents* DuplicateTabAt(BrowserWindowInterface* browser,
     CreateAndShowNewWindowWithContents(std::move(contents_dupe), browser);
   }
 
+  // Roamex (roam-14, §4.2): a duplicated tab INHERITS the initial URL
+  // (value + edit-lock); the tab uid re-mints independently.
+  roamex::tabs::TabInitialUrlHelper::OnTabDuplicated(contents,
+                                                     raw_contents_dupe);
+
   SessionServiceBase* session_service =
       GetAppropriateSessionServiceIfExisting(browser);
   if (session_service) {
diff --git a/chrome/browser/ui/tabs/tab_menu_model.h b/chrome/browser/ui/tabs/tab_menu_model.h
index ef54948fc4..60aaaa0534 100644
--- a/chrome/browser/ui/tabs/tab_menu_model.h
+++ b/chrome/browser/ui/tabs/tab_menu_model.h
@@ -86,10 +86,14 @@ class TabMenuModel : public ui::SimpleMenuModel {
       add_to_existing_comparison_table_submenu_;
   std::unique_ptr<ui::SimpleMenuModel> swap_with_split_submenu_;
   std::unique_ptr<ui::SimpleMenuModel> arrange_split_view_submenu_;
+  // Roamex (roam-14): owns the flag-gated "Initial URL" submenu.
+  std::unique_ptr<ui::SimpleMenuModel> roamex_initial_url_submenu_;
   std::unique_ptr<ui::SimpleMenuModel> glic_tab_sub_menu_model_;
   std::unique_ptr<ui::SimpleMenuModel> send_tab_to_self_submenu_;
   std::unique_ptr<send_tab_to_self::SendTabToSelfContextMenuDelegate>
       send_tab_to_self_submenu_delegate_;
+  // Roamex (roam-6): owns the flag-gated "Tab strip position" submenu.
+  std::unique_ptr<ui::SimpleMenuModel> roamex_tab_strip_position_submenu_;
 
   // `tab_strip_` needs to outlive this class.
   raw_ptr<TabStripModel> tab_strip_;
diff --git a/chrome/browser/ui/tabs/tab_menu_model.cc b/chrome/browser/ui/tabs/tab_menu_model.cc
index fd5792d295..ce6e9df36e 100644
--- a/chrome/browser/ui/tabs/tab_menu_model.cc
+++ b/chrome/browser/ui/tabs/tab_menu_model.cc
@@ -23,6 +23,7 @@
 #include "chrome/browser/glic/public/service/glic_instance_coordinator.h"
 #include "chrome/browser/glic/resources/grit/glic_browser_resources.h"
 #include "chrome/browser/profiles/profile.h"
+#include "roamex/browser/ui/tabs/tab_strip_position_menu.h"
 #include "chrome/browser/send_tab_to_self/send_tab_to_self_util.h"
 #include "chrome/browser/ui/browser_window/public/browser_window_features.h"
 #include "chrome/browser/ui/browser_window/public/browser_window_interface.h"
@@ -36,6 +37,7 @@
 #include "chrome/browser/ui/tabs/split_tab_swap_menu_model.h"
 #include "chrome/browser/ui/tabs/tab_group_model.h"
 #include "chrome/browser/ui/tabs/tab_menu_model_delegate.h"
+#include "roamex/browser/ui/tabs/initial_url_menu.h"
 #include "chrome/browser/ui/tabs/tab_strip_model.h"
 #include "chrome/browser/ui/tabs/tab_strip_model_delegate.h"
 #include "chrome/browser/ui/tabs/tab_utils.h"
@@ -344,7 +346,11 @@ void TabMenuModel::Build(int index) {
     }
   }
 
-  if (tabs::kVerticalTabsToggleInTabContextMenu.Get() && controller) {
+  // Roamex (roam-8): only offer the toggle when it can act — upstream feature
+  // on, or a roamex-driven vertical display to switch back from.
+  if (tabs::kVerticalTabsToggleInTabContextMenu.Get() && controller &&
+      (tabs::IsVerticalTabsFeatureEnabled() ||
+       controller->ShouldDisplayVerticalTabs())) {
     // TODO(crbug.com/475222200): When in immersive, swapping between tab
     // strip types create duplicate tab strips. Until that is resolved,
     // disable the ability to swap between tab strips while in immersive.
@@ -392,6 +398,10 @@ void TabMenuModel::Build(int index) {
   }
 #endif
 
+  // Roamex (roam-14): the flag-gated "Initial URL" submenu (§4.5).
+  roamex_initial_url_submenu_ = roamex::tabs::MaybeAppendInitialUrlSubMenu(
+      this, tab_strip_->GetWebContentsAt(index));
+
   // Separator Close Tab items
   AddSeparator(ui::NORMAL_SEPARATOR);
   AddItemWithStringId(TabStripModel::CommandCloseTab, IDS_TAB_CXMENU_CLOSETAB);
@@ -409,6 +419,11 @@ void TabMenuModel::Build(int index) {
   SetEnabledAt(GetItemCount() - 1,
                tab_strip_->IsContextMenuCommandEnabled(
                    index, TabStripModel::CommandCloseTabsToRight));
+
+  // Roamex (roam-6): the flag-gated tab-strip position submenu.
+  roamex_tab_strip_position_submenu_ =
+      roamex::tabs::MaybeAppendTabStripPositionSubMenu(
+          this, tab_strip_->profile()->GetPrefs());
 }
 
 DEFINE_CLASS_ELEMENT_IDENTIFIER_VALUE(TabMenuModel,
diff --git a/chrome/browser/ui/tabs/BUILD.gn b/chrome/browser/ui/tabs/BUILD.gn
index 0f5a24dcec..a236c49bce 100644
--- a/chrome/browser/ui/tabs/BUILD.gn
+++ b/chrome/browser/ui/tabs/BUILD.gn
@@ -94,6 +94,12 @@ if (!is_android) {
     sources = [
       "glic_tab_sub_menu_model.cc",
       "tab_menu_model.cc",
+      # Roamex (roam-14): the initial-URL submenu + edit dialog
+      # (roamex-owned files compiled into this target, 0009-precedent).
+      "//roamex/browser/ui/tabs/edit_initial_url_dialog.cc",
+      "//roamex/browser/ui/tabs/edit_initial_url_dialog.h",
+      "//roamex/browser/ui/tabs/initial_url_menu.cc",
+      "//roamex/browser/ui/tabs/initial_url_menu.h",
       "tab_menu_model_factory.cc",
     ]
 
@@ -117,6 +123,7 @@ if (!is_android) {
       "//components/commerce/core:commerce_constants",
       "//components/commerce/core:feature_list",
       "//components/feed:feature_list",
+      "//roamex/browser/ui/tabs",
     ]
 
     # TODO(crbug.com/40031409): Fix code that adds exit-time destructors and
@@ -480,6 +487,7 @@ if (!is_android) {
     }
 
     deps = [
+      "//roamex/common",
       ":tab_group",
       ":tab_model",
       ":tab_strip",
@@ -605,6 +613,20 @@ if (!is_android) {
     if (is_chromeos) {
       deps += [ "//chrome/browser/apps/app_service" ]
     }
+
+    # Roamex (roam-10): the chrome-facing uid helper/factory are roamex-owned
+    # files compiled into this target (they need Profile/SessionService; a
+    # separate roamex target would create a GN cycle). The pure registry lives
+    # in //roamex/browser/tabs.
+    sources += [
+      "//roamex/browser/tabs/tab_initial_url_helper.cc",
+      "//roamex/browser/tabs/tab_initial_url_helper.h",
+      "//roamex/browser/tabs/tab_uid_service_factory.cc",
+      "//roamex/browser/tabs/tab_uid_service_factory.h",
+      "//roamex/browser/tabs/tab_uid_tab_helper.cc",
+      "//roamex/browser/tabs/tab_uid_tab_helper.h",
+    ]
+    deps += [ "//roamex/browser/tabs" ]
   }
 
   # This is a separate component to avoid possible circular deps.
