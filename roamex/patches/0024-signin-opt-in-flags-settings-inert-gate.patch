--- a/chrome/browser/signin/signin_ui_util.cc
+++ b/chrome/browser/signin/signin_ui_util.cc
@@ -4,6 +4,8 @@
 
 #include "chrome/browser/signin/signin_ui_util.h"
 
+#include "roamex/browser/ui/signin/signin_inert_dialog.h"
+
 #include "base/auto_reset.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
@@ -179,6 +181,11 @@
 void ShowReauthForPrimaryAccountWithAuthError(
     Profile* profile,
     signin_metrics::AccessPoint access_point) {
+  // Roamex (roam-31): keyless / keyed-but-unauthorized builds intercept
+  // sign-in initiation with an explanation (never a silent dead button).
+  if (roamex::signin_ui::MaybeInterceptInertSignin(profile)) {
+    return;
+  }
   signin::IdentityManager* identity_manager =
       IdentityManagerFactory::GetForProfile(profile);
   CoreAccountInfo primary_account_info =
@@ -193,6 +200,11 @@
 void ShowReauthForAccount(Profile* profile,
                           const std::string& email,
                           signin_metrics::AccessPoint access_point) {
+  // Roamex (roam-31): keyless / keyed-but-unauthorized builds intercept
+  // sign-in initiation with an explanation (never a silent dead button).
+  if (roamex::signin_ui::MaybeInterceptInertSignin(profile)) {
+    return;
+  }
 #if BUILDFLAG(IS_CHROMEOS)
   ash::AccountManagerFactory::Get()
       ->GetAccountManagerFacade(profile->GetPath().value())
@@ -212,6 +224,11 @@
 void ShowExtensionSigninPrompt(Profile* profile,
                                bool enable_sync,
                                const std::string& email_hint) {
+  // Roamex (roam-31): keyless / keyed-but-unauthorized builds intercept
+  // sign-in initiation with an explanation (never a silent dead button).
+  if (roamex::signin_ui::MaybeInterceptInertSignin(profile)) {
+    return;
+  }
 #if BUILDFLAG(IS_CHROMEOS)
   NOTREACHED();
 #elif BUILDFLAG(ENABLE_DICE_SUPPORT)
@@ -249,6 +266,11 @@
 
 void ShowSigninPromptFromPromo(Profile* profile,
                                signin_metrics::AccessPoint access_point) {
+  // Roamex (roam-31): keyless / keyed-but-unauthorized builds intercept
+  // sign-in initiation with an explanation (never a silent dead button).
+  if (roamex::signin_ui::MaybeInterceptInertSignin(profile)) {
+    return;
+  }
 #if BUILDFLAG(IS_CHROMEOS)
   NOTREACHED();
 #elif BUILDFLAG(ENABLE_DICE_SUPPORT)
@@ -271,6 +293,11 @@
 void SignInFromSingleAccountPromo(Profile* profile,
                                   const CoreAccountInfo& account,
                                   signin_metrics::AccessPoint access_point) {
+  // Roamex (roam-31): keyless / keyed-but-unauthorized builds intercept
+  // sign-in initiation with an explanation (never a silent dead button).
+  if (roamex::signin_ui::MaybeInterceptInertSignin(profile)) {
+    return;
+  }
 #if BUILDFLAG(ENABLE_DICE_SUPPORT)
   DCHECK(!profile->IsOffTheRecord());
 
@@ -331,6 +358,11 @@
     Profile* profile,
     const CoreAccountInfo& account,
     signin_metrics::AccessPoint access_point) {
+  // Roamex (roam-31): keyless / keyed-but-unauthorized builds intercept
+  // sign-in initiation with an explanation (never a silent dead button).
+  if (roamex::signin_ui::MaybeInterceptInertSignin(profile)) {
+    return;
+  }
   EnableSyncFromMultiAccountPromo(profile, account, access_point,
                                   /*is_default_promo_account=*/true);
 }
@@ -339,6 +371,11 @@
                                      const CoreAccountInfo& account,
                                      signin_metrics::AccessPoint access_point,
                                      bool is_default_promo_account) {
+  // Roamex (roam-31): keyless / keyed-but-unauthorized builds intercept
+  // sign-in initiation with an explanation (never a silent dead button).
+  if (roamex::signin_ui::MaybeInterceptInertSignin(profile)) {
+    return;
+  }
 #if BUILDFLAG(ENABLE_DICE_SUPPORT)
   DCHECK(!profile->IsOffTheRecord());
 
@@ -646,6 +683,11 @@
 void SignInAndEnableHistorySync(BrowserWindowInterface* browser,
                                 Profile* profile,
                                 signin_metrics::AccessPoint access_point) {
+  // Roamex (roam-31): see MaybeInterceptInertSignin above.
+  if (roamex::signin_ui::MaybeInterceptInertSignin(
+          browser ? browser->GetProfile() : nullptr)) {
+    return;
+  }
 #if !BUILDFLAG(IS_CHROMEOS)
   syncer::SyncService* sync_service =
       SyncServiceFactory::GetForProfile(profile);
--- a/chrome/browser/signin/BUILD.gn
+++ b/chrome/browser/signin/BUILD.gn
@@ -203,6 +203,7 @@
   ]
   deps = [
     "//roamex/browser/signin",
+    "//components/constrained_window",
     "//chrome/browser:global_features",
     "//chrome/browser/autofill",
     "//chrome/browser/bookmarks",
@@ -318,6 +319,8 @@
       "signin_promo.cc",
       "signin_promo_util.cc",
       "signin_ui_util.cc",
+      "//roamex/browser/ui/signin/signin_inert_dialog.cc",
+      "//roamex/browser/ui/signin/signin_inert_dialog.h",
     ]
     deps += [
       ":signin_promo",
--- a/chrome/browser/about_flags.cc
+++ b/chrome/browser/about_flags.cc
@@ -6,6 +6,8 @@
 // https://chromium.googlesource.com/chromium/src/+/main/docs/how_to_add_your_feature_flag.md#step-2_adding-the-feature-flag-to-the-chrome_flags-ui
 
 #include "chrome/browser/about_flags.h"
+
+#include "roamex/common/roamex_switches.h"
 
 #include <iterator>
 #include <map>
@@ -4740,6 +4742,15 @@
 //
 // When adding a new choice, add it to the end of the list.
 const FeatureEntry kFeatureEntries[] = {
+    // Roamex (roam-31): developer mirror of
+    // roamex.signin.optional_entry_point_enabled (E5). Functional sign-in
+    // additionally requires a keyed-and-authorized build (three-state
+    // honesty, plan §7.3).
+    {"roamex-signin-opt-in", "Roamex: optional sign-in entry point",
+     "Re-exposes the Chromium sign-in/sync surfaces on the next launch, as if "
+     "the Roamex opt-in preference were enabled. Sign-in stays "
+     "inert-with-explanation unless the build is keyed and authorized.",
+     kOsMac | kOsWin | kOsLinux, SINGLE_VALUE_TYPE(roamex::switches::kSigninOptIn)},
 // Include generated flags for flag unexpiry; see //docs/flag_expiry.md and
 // //tools/flags/generate_unexpire_flags.py.
 #include "build/chromeos_buildflags.h"
--- a/chrome/browser/flag-metadata.json
+++ b/chrome/browser/flag-metadata.json
@@ -8429,6 +8429,11 @@
       "expiry_milestone": 150
   },
   {
+    "name": "roamex-signin-opt-in",
+    "owners": [ "xinquan568" ],
+    "expiry_milestone": -1
+  },
+  {
     "name": "saas-usage-reporting",
     "owners": [ "janzarzycki@google.com", "igorruvinov@chromium.org" ],
     "expiry_milestone": 152
--- a/chrome/browser/extensions/api/settings_private/prefs_util.cc
+++ b/chrome/browser/extensions/api/settings_private/prefs_util.cc
@@ -206,6 +206,9 @@
   s_allowlist = new PrefsUtil::TypedPrefMap();
 
   // Roamex (roam-6; the key literal mirrors roamex/common/roamex_prefs.h).
+  // Roamex (roam-31): the E5 sign-in opt-in toggle.
+  (*s_allowlist)["roamex.signin.optional_entry_point_enabled"] =
+      settings_api::PrefType::kBoolean;
   (*s_allowlist)["roamex.tabs.strip_position"] =
       settings_api::PrefType::kNumber;
 
--- a/chrome/browser/resources/settings/appearance_page/appearance_page.html
+++ b/chrome/browser/resources/settings/appearance_page/appearance_page.html
@@ -180,6 +180,14 @@
                 menu-options="[[roamexTabStripPositionOptions_]]">
             </settings-dropdown-menu>
           </div>
+        </template>
+
+        <template is="dom-if" if="[[roamexSigninOptInEnabled_]]">
+          <settings-toggle-button id="roamexSigninOptIn"
+              pref="{{prefs.roamex.signin.optional_entry_point_enabled}}"
+              label="Optional Google sign-in entry point (Roamex)"
+              sub-label="Off by default. Re-exposes the Chromium sign-in/sync surfaces after you relaunch. Functional sign-in additionally requires a build with Google API keys and Google authorization; otherwise the surfaces explain why sign-in is unavailable.">
+          </settings-toggle-button>
         </template>
 
         <template is="dom-if" if="[[showVerticalTabsEnabled_]]">
--- a/chrome/browser/resources/settings/appearance_page/appearance_page.ts
+++ b/chrome/browser/resources/settings/appearance_page/appearance_page.ts
@@ -242,6 +242,14 @@
         },
       },
 
+      // Roamex (roam-31): the E5 sign-in opt-in toggle rides the same page.
+      roamexSigninOptInEnabled_: {
+        type: Boolean,
+        value() {
+          return loadTimeData.getBoolean('roamexSigninOptInEnabled');
+        },
+      },
+
       roamexTabStripPositionOptions_: {
         type: Array,
         value() {
@@ -333,6 +341,7 @@
   declare private isWallpaperPolicyControlled_: boolean;
   declare private fontSizeOptions_: DropdownMenuOptionList;
   declare private roamexTabStripPositionEnabled_: boolean;
+  declare private roamexSigninOptInEnabled_: boolean;
   declare private roamexTabStripPositionOptions_: DropdownMenuOptionList;
   declare private colorSchemeModeOptions_:
       Array<{value: ColorSchemeMode, name: string}>;
--- a/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc
+++ b/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc
@@ -589,6 +589,11 @@
   html_source->AddBoolean("roamexTabStripPositionEnabled",
                           base::FeatureList::IsEnabled(
                               roamex::features::kTabStripPosition));
+  // Roamex (roam-31): the E5 opt-in toggle is visible iff the epic flag is
+  // on (it must stay reachable while the sign-in surfaces are suppressed).
+  html_source->AddBoolean("roamexSigninOptInEnabled",
+                          base::FeatureList::IsEnabled(
+                              roamex::features::kBraveStyleProfiles));
   html_source->AddBoolean("showVerticalTabsExpandOnHoverEnabled",
                           tabs::IsVerticalTabsExpandOnHoverFeatureEnabled());
   html_source->AddBoolean(
