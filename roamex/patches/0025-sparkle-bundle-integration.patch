--- a/chrome/BUILD.gn
+++ b/chrome/BUILD.gn
@@ -47,6 +47,7 @@
   import("//build/config/apple/symbols.gni")
   import("//build/config/mac/mac_sdk.gni")
   import("//build/config/mac/rules.gni")
+import("//roamex/build/buildflags.gni")
   import("//build/util/branding.gni")
   import("//chrome/browser/buildflags.gni")
   import("//chrome/updater/branding.gni")
@@ -490,8 +491,13 @@
     }
   }
 
+  # Roamex (roam-32): merge Sparkle keys into the app plist when the flag is on.
+  _roamex_app_plists = [ "app/app-Info.plist" ]
+  if (roamex_enable_sparkle) {
+    _roamex_app_plists += [ "//roamex/app/sparkle-Info.plist" ]
+  }
   tweak_info_plist("chrome_app_plist") {
-    info_plist = "app/app-Info.plist"
+    info_plists = _roamex_app_plists
     args = [
       "--breakpad=0",
       "--scm=1",
@@ -527,6 +533,8 @@
     configs += [ "//build/config/compiler:wexit_time_destructors" ]
 
     deps = [
+      # Roamex (roam-32): copy Sparkle.framework into Contents/Frameworks.
+      "//roamex/app",
       ":chrome_app_strings_bundle_data",
       ":chrome_resources",
       ":chrome_versioned_bundle_data",
--- a/chrome/app/generated_resources.grd
+++ b/chrome/app/generated_resources.grd
@@ -15809,6 +15809,10 @@
           Help
         </message>
         <!-- App menu -->
+        <!-- Roamex (roam-32): the Sparkle "Check for Updates…" menu item. -->
+        <message name="IDS_ROAMEX_CHECK_FOR_UPDATES" desc="Mac menu item to check for Roamex app updates." translateable="false">
+          Check for Updates…
+        </message>
         <message name="IDS_ABOUT_MAC" desc="The Mac menu item to open the about box.">
           About <ph name="PRODUCT_NAME">$1<ex>Google Chrome</ex></ph>
         </message>
--- a/chrome/browser/BUILD.gn
+++ b/chrome/browser/BUILD.gn
@@ -9,6 +9,7 @@
 import("//build/config/chromebox_for_meetings/buildflags.gni")
 import("//build/config/compiler/pgo/pgo.gni")
 import("//build/config/features.gni")
+import("//roamex/build/buildflags.gni")
 import("//build/config/python.gni")
 import("//build/config/ui.gni")
 import("//chrome/browser/buildflags.gni")
@@ -649,6 +650,8 @@
   ]
 
   deps = [
+    "//roamex/app:app_buildflags",
+    "//roamex/common",
     ":active_use_util",
     ":browser_features",
     ":browser_process",
@@ -1656,6 +1659,13 @@
     "//chrome/browser/ui/webauthn:impl",
   ]
 
+  # Roamex (roam-32): app_controller_mac.mm calls the Sparkle updater on
+  # flag-on builds; pull its definitions (the buildflag header alone is not
+  # enough to link).
+  if (roamex_enable_sparkle) {
+    deps += [ "//roamex/app:sparkle_updater" ]
+  }
+
   if (enable_webui_ntp) {
     deps += [
       "//chrome/browser/new_tab_page",
@@ -4195,6 +4205,11 @@
       "fullscreen_mac.mm",
       "global_keyboard_shortcuts_mac.h",
       "global_keyboard_shortcuts_mac.mm",
+      # Roamex (roam-13): the shared shortcut surface (mac-only product).
+      "//roamex/browser/tabs/shortcut_registry_mac.h",
+      "//roamex/browser/tabs/shortcut_registry_mac.mm",
+      "//roamex/browser/ui/webui/roamex_shortcuts_handler.cc",
+      "//roamex/browser/ui/webui/roamex_shortcuts_handler.h",
       "icon_loader_mac.mm",
       "memory_details_mac.cc",
       "platform_util_mac.mm",
--- a/chrome/browser/app_controller_mac.mm
+++ b/chrome/browser/app_controller_mac.mm
@@ -3,6 +3,11 @@
 // found in the LICENSE file.
 
 #import "chrome/browser/app_controller_mac.h"
+
+#include "roamex/app/app_buildflags.h"
+#if BUILDFLAG(ROAMEX_ENABLE_SPARKLE)
+#include "roamex/app/roamex_sparkle_updater.h"
+#endif
 
 #include <AppKit/AppKit.h>
 #include <dispatch/dispatch.h>
@@ -1252,7 +1257,19 @@
 
 // This is called after profiles have been loaded and preferences registered.
 // It is safe to access the default profile here.
+#if BUILDFLAG(ROAMEX_ENABLE_SPARKLE)
+// Roamex (roam-32): the "Check for Updates…" menu action.
+- (void)roamexCheckForUpdates:(id)sender {
+  roamex::app::CheckForUpdates();
+}
+#endif
+
 - (void)applicationDidFinishLaunching:(NSNotification*)notify {
+#if BUILDFLAG(ROAMEX_ENABLE_SPARKLE)
+  // Roamex (roam-32): start the Sparkle updater (scheduled checks per the
+  // Info.plist keys merged into the bundle).
+  roamex::app::InitSparkleUpdater();
+#endif
   if (g_browser_process->browser_policy_connector()
           ->chrome_browser_cloud_management_controller()
           ->IsEnterpriseStartupDialogShowing()) {
--- a/chrome/browser/ui/cocoa/BUILD.gn
+++ b/chrome/browser/ui/cocoa/BUILD.gn
@@ -127,6 +127,9 @@
 
   public_deps = [ "//chrome/browser:browser_public_dependencies" ]
 
+  # Roamex (roam-32): the ROAMEX_ENABLE_SPARKLE buildflag for the menu item.
+  deps = [ "//roamex/app:app_buildflags" ]
+
   deps += [
     "//chrome/app:command_ids",
     "//chrome/app/theme:chrome_unscaled_resources_grit",
--- a/chrome/browser/ui/cocoa/main_menu_builder.mm
+++ b/chrome/browser/ui/cocoa/main_menu_builder.mm
@@ -4,6 +4,8 @@
 
 #import "chrome/browser/ui/cocoa/main_menu_builder.h"
 
+#include "roamex/app/app_buildflags.h"
+
 #include "base/feature_list.h"
 #include "base/i18n/rtl.h"
 #include "base/mac/mac_util.h"
@@ -61,6 +63,12 @@
                   .tag(IDC_ABOUT)
                   .target(app_delegate)
                   .action(@selector(orderFrontStandardAboutPanel:)),
+#if BUILDFLAG(ROAMEX_ENABLE_SPARKLE)
+              // Roamex (roam-32): Sparkle "Check for Updates…".
+              Item(IDS_ROAMEX_CHECK_FOR_UPDATES)
+                  .target(app_delegate)
+                  .action(@selector(roamexCheckForUpdates:)),
+#endif
               Item().is_separator(),
               Item(IDS_PREFERENCES)
                   .tag(IDC_OPTIONS)
