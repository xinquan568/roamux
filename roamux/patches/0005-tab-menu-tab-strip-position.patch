diff --git a/chrome/browser/ui/tabs/BUILD.gn b/chrome/browser/ui/tabs/BUILD.gn
index 0f5a24dcec..1477a4ea0b 100644
--- a/chrome/browser/ui/tabs/BUILD.gn
+++ b/chrome/browser/ui/tabs/BUILD.gn
@@ -117,6 +117,7 @@ if (!is_android) {
       "//components/commerce/core:commerce_constants",
       "//components/commerce/core:feature_list",
       "//components/feed:feature_list",
+      "//roamux/browser/ui/tabs",
     ]
 
     # TODO(crbug.com/40031409): Fix code that adds exit-time destructors and
diff --git a/chrome/browser/ui/tabs/tab_menu_model.cc b/chrome/browser/ui/tabs/tab_menu_model.cc
index fd5792d295..b933679e4c 100644
--- a/chrome/browser/ui/tabs/tab_menu_model.cc
+++ b/chrome/browser/ui/tabs/tab_menu_model.cc
@@ -23,6 +23,7 @@
 #include "chrome/browser/glic/public/service/glic_instance_coordinator.h"
 #include "chrome/browser/glic/resources/grit/glic_browser_resources.h"
 #include "chrome/browser/profiles/profile.h"
+#include "roamux/browser/ui/tabs/tab_strip_position_menu.h"
 #include "chrome/browser/send_tab_to_self/send_tab_to_self_util.h"
 #include "chrome/browser/ui/browser_window/public/browser_window_features.h"
 #include "chrome/browser/ui/browser_window/public/browser_window_interface.h"
@@ -409,6 +410,11 @@ void TabMenuModel::Build(int index) {
   SetEnabledAt(GetItemCount() - 1,
                tab_strip_->IsContextMenuCommandEnabled(
                    index, TabStripModel::CommandCloseTabsToRight));
+
+  // Roamux (roam-6): the flag-gated tab-strip position submenu.
+  roamux_tab_strip_position_submenu_ =
+      roamux::tabs::MaybeAppendTabStripPositionSubMenu(
+          this, tab_strip_->profile()->GetPrefs());
 }
 
 DEFINE_CLASS_ELEMENT_IDENTIFIER_VALUE(TabMenuModel,
diff --git a/chrome/browser/ui/tabs/tab_menu_model.h b/chrome/browser/ui/tabs/tab_menu_model.h
index ef54948fc4..689614160b 100644
--- a/chrome/browser/ui/tabs/tab_menu_model.h
+++ b/chrome/browser/ui/tabs/tab_menu_model.h
@@ -90,6 +90,8 @@ class TabMenuModel : public ui::SimpleMenuModel {
   std::unique_ptr<ui::SimpleMenuModel> send_tab_to_self_submenu_;
   std::unique_ptr<send_tab_to_self::SendTabToSelfContextMenuDelegate>
       send_tab_to_self_submenu_delegate_;
+  // Roamux (roam-6): owns the flag-gated "Tab strip position" submenu.
+  std::unique_ptr<ui::SimpleMenuModel> roamux_tab_strip_position_submenu_;
 
   // `tab_strip_` needs to outlive this class.
   raw_ptr<TabStripModel> tab_strip_;
