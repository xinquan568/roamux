diff --git a/chrome/browser/extensions/api/settings_private/prefs_util.cc b/chrome/browser/extensions/api/settings_private/prefs_util.cc
index e81d7f44bf..94877a0306 100644
--- a/chrome/browser/extensions/api/settings_private/prefs_util.cc
+++ b/chrome/browser/extensions/api/settings_private/prefs_util.cc
@@ -205,6 +205,10 @@ const PrefsUtil::TypedPrefMap& PrefsUtil::GetAllowlistedKeys() {
   }
   s_allowlist = new PrefsUtil::TypedPrefMap();
 
+  // Roamux (roam-6; the key literal mirrors roamux/common/roamux_prefs.h).
+  (*s_allowlist)["roamux.tabs.strip_position"] =
+      settings_api::PrefType::kNumber;
+
   // Miscellaneous
   (*s_allowlist)[::embedder_support::kAlternateErrorPagesEnabled] =
       settings_api::PrefType::kBoolean;
diff --git a/chrome/browser/resources/settings/appearance_page/appearance_page.html b/chrome/browser/resources/settings/appearance_page/appearance_page.html
index 89e745d043..5e0a09805b 100644
--- a/chrome/browser/resources/settings/appearance_page/appearance_page.html
+++ b/chrome/browser/resources/settings/appearance_page/appearance_page.html
@@ -169,6 +169,19 @@
             label="$i18n{showBookmarksBar}">
         </settings-toggle-button>
 
+        <template is="dom-if" if="[[roamuxTabStripPositionEnabled_]]">
+          <div class="cr-row">
+            <div class="flex cr-padded-text" aria-hidden="true">
+              Tab strip position (Roamux)
+            </div>
+            <settings-dropdown-menu id="roamuxTabStripPosition"
+                label="Tab strip position (Roamux)"
+                pref="{{prefs.roamux.tabs.strip_position}}"
+                menu-options="[[roamuxTabStripPositionOptions_]]">
+            </settings-dropdown-menu>
+          </div>
+        </template>
+
         <template is="dom-if" if="[[showVerticalTabsEnabled_]]">
           <div class="cr-row">
             <div class="flex cr-padded-text" aria-hidden="true">
diff --git a/chrome/browser/resources/settings/appearance_page/appearance_page.ts b/chrome/browser/resources/settings/appearance_page/appearance_page.ts
index 7e49761..ea759d4 100644
--- a/chrome/browser/resources/settings/appearance_page/appearance_page.ts
+++ b/chrome/browser/resources/settings/appearance_page/appearance_page.ts
@@ -230,7 +230,30 @@ export class SettingsAppearancePageElement extends
       showVerticalTabsEnabled_: {
         type: Boolean,
         value() {
-          return loadTimeData.getBoolean('showVerticalTabsEnabled');
+          // Roamux (roam-183): the roamux placement is the sole tab-position
+          // control when its flag is on, so the upstream Vertical/Horizontal
+          // row (this property's only consumer) is suppressed.
+          return loadTimeData.getBoolean('showVerticalTabsEnabled') &&
+              !loadTimeData.getBoolean('roamuxTabStripPositionEnabled');
+        },
+      },
+
+      roamuxTabStripPositionEnabled_: {
+        type: Boolean,
+        value() {
+          return loadTimeData.getBoolean('roamuxTabStripPositionEnabled');
+        },
+      },
+
+      roamuxTabStripPositionOptions_: {
+        type: Array,
+        value() {
+          return [
+            {value: 0, name: 'Top'},
+            {value: 1, name: 'Bottom'},
+            {value: 2, name: 'Left'},
+            {value: 3, name: 'Right'},
+          ];
         },
       },
 
@@ -288,6 +311,8 @@ export class SettingsAppearancePageElement extends
   declare private defaultZoom_: number;
   declare private isWallpaperPolicyControlled_: boolean;
   declare private fontSizeOptions_: DropdownMenuOptionList;
+  declare private roamuxTabStripPositionEnabled_: boolean;
+  declare private roamuxTabStripPositionOptions_: DropdownMenuOptionList;
   declare private colorSchemeModeOptions_:
       Array<{value: ColorSchemeMode, name: string}>;
   declare private selectedColorSchemeMode_: ColorSchemeMode|undefined;
diff --git a/chrome/browser/ui/BUILD.gn b/chrome/browser/ui/BUILD.gn
index 5ef79ef4e8..a087bb5eac 100644
--- a/chrome/browser/ui/BUILD.gn
+++ b/chrome/browser/ui/BUILD.gn
@@ -377,6 +377,7 @@ static_library("ui") {
     "//chrome/browser/preloading/prefetch/no_state_prefetch",
     "//chrome/browser/privacy_sandbox:headers",
     "//chrome/browser/profiles",
+    "//roamux/common",
     "//chrome/browser/profiles:misc",
     "//chrome/browser/profiles:profile",
     "//chrome/browser/profiles:profile_manager",
diff --git a/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc b/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc
index b8170f520a..e77aea98dd 100644
--- a/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc
+++ b/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc
@@ -48,6 +48,7 @@
 #include "chrome/browser/ui/tabs/tab_strip_prefs.h"
 #include "chrome/browser/ui/toasts/toast_features.h"
 #include "chrome/browser/ui/ui_features.h"
+#include "roamux/common/roamux_features.h"
 #include "chrome/browser/ui/webui/management/management_ui.h"
 #include "chrome/browser/ui/webui/policy_indicator_localized_strings_provider.h"
 #include "chrome/browser/ui/webui/settings/glic_handler.h"
@@ -582,6 +583,9 @@ void AddAppearanceStrings(content::WebUIDataSource* html_source,
       base::FeatureList::IsEnabled(features::kTabHoverCardImages));
   html_source->AddBoolean("showVerticalTabsEnabled",
                           tabs::IsVerticalTabsFeatureEnabled());
+  html_source->AddBoolean("roamuxTabStripPositionEnabled",
+                          base::FeatureList::IsEnabled(
+                              roamux::features::kTabStripPosition));
   html_source->AddBoolean("showVerticalTabsExpandOnHoverEnabled",
                           tabs::IsVerticalTabsExpandOnHoverFeatureEnabled());
   html_source->AddBoolean(
