diff --git a/chrome/browser/ui/browser_window/internal/BUILD.gn b/chrome/browser/ui/browser_window/internal/BUILD.gn
index be1c5ca3b5..b43ca0a0a4 100644
--- a/chrome/browser/ui/browser_window/internal/BUILD.gn
+++ b/chrome/browser/ui/browser_window/internal/BUILD.gn
@@ -12,6 +12,7 @@ source_set("internal") {
     "profile_browser_collection.cc",
   ]
   deps = [
+    "//roamux/common",
     "//base",
     "//chrome/browser:browser_process",
     "//chrome/browser:global_features",
diff --git a/chrome/browser/ui/browser_window/internal/browser_window_features.cc b/chrome/browser/ui/browser_window/internal/browser_window_features.cc
index 6aaf694106..34157ef829 100644
--- a/chrome/browser/ui/browser_window/internal/browser_window_features.cc
+++ b/chrome/browser/ui/browser_window/internal/browser_window_features.cc
@@ -79,6 +79,7 @@
 #include "chrome/browser/ui/sync/browser_synced_window_delegate.h"
 #include "chrome/browser/ui/tab_search_feature.h"
 #include "chrome/browser/ui/tabs/features.h"
+#include "roamux/common/roamux_features.h"
 #include "chrome/browser/ui/tabs/glic_actor_nudge_controller.h"
 #include "chrome/browser/ui/tabs/projects/projects_panel_state_controller.h"
 #include "chrome/browser/ui/tabs/saved_tab_groups/most_recent_shared_tab_update_store.h"
@@ -348,7 +349,11 @@ void BrowserWindowFeatures::Init(BrowserWindowInterface* browser) {
           browser, tab_list_bridge_.get());
     }
 
-    if (tabs::IsVerticalTabsFeatureEnabled()) {
+    // Roamux (roam-8): the roamux tab-strip-position flag also needs the
+    // vertical-tabs state controller (display is decided later by
+    // ShouldDisplayVerticalTabs).
+    if (tabs::IsVerticalTabsFeatureEnabled() ||
+        base::FeatureList::IsEnabled(roamux::features::kTabStripPosition)) {
       Browser* raw_browser = browser->GetBrowserForMigrationOnly();
 
       std::optional<bool> restored_state_collapsed =
diff --git a/chrome/browser/ui/tabs/vertical_tab_strip_state_controller.cc b/chrome/browser/ui/tabs/vertical_tab_strip_state_controller.cc
index 9919d3a042..b206814076 100644
--- a/chrome/browser/ui/tabs/vertical_tab_strip_state_controller.cc
+++ b/chrome/browser/ui/tabs/vertical_tab_strip_state_controller.cc
@@ -6,6 +6,7 @@
 
 #include <optional>
 
+#include "base/feature_list.h"
 #include "base/i18n/rtl.h"
 #include "base/metrics/user_metrics.h"
 #include "base/metrics/user_metrics_action.h"
@@ -30,6 +31,9 @@
 #include "components/prefs/pref_notifier_impl.h"
 #include "components/prefs/pref_service.h"
 #include "components/sessions/core/session_id.h"
+#include "roamux/common/roamux_features.h"
+#include "roamux/common/roamux_prefs.h"
+#include "roamux/common/tab_strip_placement.h"
 #include "ui/actions/actions.h"
 #include "ui/base/l10n/l10n_util.h"
 #include "ui/views/vector_icons.h"
@@ -63,6 +67,17 @@ VerticalTabStripStateController::VerticalTabStripStateController(
       base::BindRepeating(&VerticalTabStripStateController::OnModeChanged,
                           base::Unretained(this)));
 
+  // Roamux (roam-8): placement left/right rides the same mode-change
+  // pipeline, but only when the effective display mode actually flips.
+  pref_change_registrar_.Add(
+      roamux::prefs::kTabStripPosition,
+      base::BindRepeating(
+          &VerticalTabStripStateController::OnRoamuxPlacementChanged,
+          base::Unretained(this)));
+  roamux_placement_wants_vertical_ =
+      roamux::ShouldDisplayVerticalTabsForPlacement(pref_service_);
+  roamux_last_display_vertical_ = ShouldDisplayVerticalTabs();
+
   if (IsVerticalTabsExpandOnHoverFeatureEnabled()) {
     is_expand_on_hover_enabled_ =
         pref_service_->GetBoolean(prefs::kVerticalTabsExpandOnHoverEnabled);
@@ -174,10 +189,29 @@ void VerticalTabStripStateController::SetDelegate(Delegate* delegate) {
 }
 
 bool VerticalTabStripStateController::ShouldDisplayVerticalTabs() const {
+  // Roamux (roam-182): with the roamux flag on, the placement is the SOLE
+  // authority for vertical display — the upstream pref/feature contribution is
+  // ignored (ORing it in was the "placement does nothing" bug when upstream
+  // vertical tabs were on). Reads the cached placement contribution so the
+  // answer stays frozen while an enable-state lock is held.
+  if (base::FeatureList::IsEnabled(roamux::features::kTabStripPosition)) {
+    return roamux_placement_wants_vertical_;
+  }
   return IsVerticalTabsFeatureEnabled() && is_vertical_tabs_enabled_;
 }
 
 void VerticalTabStripStateController::SetVerticalTabsEnabled(bool enabled) {
+  // Roamux (roam-182): with the flag on, the roamux placement is the sole
+  // authority, so the upstream "switch vertical/horizontal" toggle maps onto
+  // placement (vertical -> Left, horizontal -> Top) instead of writing the
+  // upstream pref, which the display decision no longer reads. The placement
+  // write drives the swap via OnRoamuxPlacementChanged.
+  if (base::FeatureList::IsEnabled(roamux::features::kTabStripPosition)) {
+    roamux::SetTabStripPlacement(pref_service_,
+                                 enabled ? roamux::TabStripPlacement::kLeft
+                                         : roamux::TabStripPlacement::kTop);
+    return;
+  }
   NotifyModeWillChange();
   pref_service_->SetBoolean(prefs::kVerticalTabsEnabled, enabled);
 }
@@ -298,9 +332,29 @@ void VerticalTabStripStateController::OnModeChanged() {
   is_vertical_tabs_enabled_ =
       pref_service_->GetBoolean(prefs::kVerticalTabsEnabled);
 
+  // Roamux (roam-8): keep the cached display mode in sync on every real
+  // mode-change notification.
+  roamux_last_display_vertical_ = ShouldDisplayVerticalTabs();
+
   NotifyModeChanged();
 }
 
+void VerticalTabStripStateController::OnRoamuxPlacementChanged() {
+  if (enable_state_lock_count_ > 0) {
+    // Defer: OnLockDestroyed reconciles the effective display on unlock. The
+    // cached contribution stays frozen so layout keeps the current mode.
+    return;
+  }
+  roamux_placement_wants_vertical_ =
+      roamux::ShouldDisplayVerticalTabsForPlacement(pref_service_);
+  if (ShouldDisplayVerticalTabs() == roamux_last_display_vertical_) {
+    // Same display mode (e.g. top<->bottom or left<->right): no strip swap;
+    // layout updates ride the BrowserView-level placement observer.
+    return;
+  }
+  OnModeChanged();
+}
+
 void VerticalTabStripStateController::OnExpandOnHoverEnabledChanged() {
   is_expand_on_hover_enabled_ =
       pref_service_->GetBoolean(prefs::kVerticalTabsExpandOnHoverEnabled);
@@ -336,9 +390,17 @@ void VerticalTabStripStateController::OnLockDestroyed() {
   enable_state_lock_count_--;
 
   if (enable_state_lock_count_ == 0) {
-    bool new_state = pref_service_->GetBoolean(prefs::kVerticalTabsEnabled);
-    if (new_state != is_vertical_tabs_enabled_) {
-      is_vertical_tabs_enabled_ = new_state;
+    // Roamux (roam-8): reconcile the EFFECTIVE display — both the upstream
+    // pref and the roamux placement may have changed while locked; notify
+    // only when the effective display actually flipped (a same-mode notify
+    // would re-initialize an active tab strip).
+    is_vertical_tabs_enabled_ =
+        pref_service_->GetBoolean(prefs::kVerticalTabsEnabled);
+    roamux_placement_wants_vertical_ =
+        roamux::ShouldDisplayVerticalTabsForPlacement(pref_service_);
+    const bool new_display = ShouldDisplayVerticalTabs();
+    if (new_display != roamux_last_display_vertical_) {
+      roamux_last_display_vertical_ = new_display;
       NotifyModeChanged();
     }
   }
diff --git a/chrome/browser/ui/tabs/vertical_tab_strip_state_controller.h b/chrome/browser/ui/tabs/vertical_tab_strip_state_controller.h
index ae53c9d95a..07bcd66587 100644
--- a/chrome/browser/ui/tabs/vertical_tab_strip_state_controller.h
+++ b/chrome/browser/ui/tabs/vertical_tab_strip_state_controller.h
@@ -132,6 +132,9 @@ class VerticalTabStripStateController : public SessionServiceBaseObserver,
   void NotifyModeChanged();
 
   void OnModeChanged();
+  // Roamux (roam-8): fires the mode pipeline only when the effective display
+  // mode actually flips (placement changes within one mode are layout-only).
+  void OnRoamuxPlacementChanged();
   void OnExpandOnHoverEnabledChanged();
 
   // Directly sets the collapse state.
@@ -190,6 +193,13 @@ class VerticalTabStripStateController : public SessionServiceBaseObserver,
   base::CallbackListSubscription did_become_active_subscription_;
 
   bool is_vertical_tabs_enabled_ = false;
+  // Roamux (roam-8): last effective display mode, to suppress spurious strip
+  // re-initialization on placement changes that do not flip the mode.
+  bool roamux_last_display_vertical_ = false;
+  // Roamux (roam-8): cached placement contribution to the display decision —
+  // frozen while an enable-state lock is held (mirrors the
+  // is_vertical_tabs_enabled_ cache semantics for the upstream pref).
+  bool roamux_placement_wants_vertical_ = false;
   int enable_state_lock_count_ = 0;
 
   bool is_expand_on_hover_enabled_ = false;
diff --git a/chrome/browser/ui/toolbar/app_menu_model.cc b/chrome/browser/ui/toolbar/app_menu_model.cc
index 2f587ad..2163aea 100644
--- a/chrome/browser/ui/toolbar/app_menu_model.cc
+++ b/chrome/browser/ui/toolbar/app_menu_model.cc
@@ -76,6 +76,7 @@
 #include "chrome/browser/ui/tabs/tab_strip_model.h"
 #include "chrome/browser/ui/tabs/vertical_tab_strip_metrics.h"
 #include "chrome/browser/ui/tabs/vertical_tab_strip_state_controller.h"
+#include "roamux/common/roamux_features.h"
 #include "chrome/browser/ui/toolbar/app_menu_icon_controller.h"
 #include "chrome/browser/ui/toolbar/bookmark_sub_menu_model.h"
 #include "chrome/browser/ui/toolbar/chrome_labs/chrome_labs_prefs.h"
@@ -1007,7 +1008,17 @@ void ToolsMenuModel::Build(Browser* browser) {
   AddItemWithStringIdAndVectorIcon(this, IDC_NAME_WINDOW, IDS_NAME_WINDOW,
                                    kNameWindowIcon);
 
-  if (auto* controller = tabs::VerticalTabStripStateController::From(browser)) {
+  auto* controller = tabs::VerticalTabStripStateController::From(browser);
+  // Roamux (roam-8): with the roamux flag on, the controller exists even when
+  // the upstream vertical-tabs feature is off — only offer the upstream
+  // toggle when it can do something: feature on, or a roamux-driven vertical
+  // display to switch back from.
+  if (controller &&
+      // Roamux (roam-183): the roamux placement is the sole tab-position
+      // control when the flag is on — suppress the upstream toggle.
+      !base::FeatureList::IsEnabled(roamux::features::kTabStripPosition) &&
+      (tabs::IsVerticalTabsFeatureEnabled() ||
+       controller->ShouldDisplayVerticalTabs())) {
     // TODO(crbug.com/475222200): When in immersive, swapping between tab
     // strip types create duplicate tab strips. Until that is resolved,
     // disable the ability to swap between tab strips while in immersive.
diff --git a/chrome/browser/ui/views/frame/system_menu_model_builder.cc b/chrome/browser/ui/views/frame/system_menu_model_builder.cc
index a0431bf..90dd967 100644
--- a/chrome/browser/ui/views/frame/system_menu_model_builder.cc
+++ b/chrome/browser/ui/views/frame/system_menu_model_builder.cc
@@ -15,6 +15,7 @@
 #include "chrome/browser/ui/browser_window/public/browser_window_features.h"
 #include "chrome/browser/ui/tabs/features.h"
 #include "chrome/browser/ui/tabs/vertical_tab_strip_state_controller.h"
+#include "roamux/common/roamux_features.h"
 #include "chrome/browser/ui/toolbar/app_menu_model.h"
 #include "chrome/browser/ui/ui_features.h"
 #include "chrome/browser/ui/views/frame/immersive_mode_controller.h"
@@ -123,8 +124,15 @@ void SystemMenuModelBuilder::BuildSystemMenuForBrowserWindow(
   }
 #endif  // BUILDFLAG(IS_WIN)
 
-  if (auto* controller =
-          tabs::VerticalTabStripStateController::From(browser())) {
+  auto* controller = tabs::VerticalTabStripStateController::From(browser());
+  // Roamux (roam-8): only offer the toggle when it can act — upstream feature
+  // on, or a roamux-driven vertical display to switch back from.
+  if (controller &&
+      // Roamux (roam-183): the roamux placement is the sole tab-position
+      // control when the flag is on — suppress the upstream toggle.
+      !base::FeatureList::IsEnabled(roamux::features::kTabStripPosition) &&
+      (tabs::IsVerticalTabsFeatureEnabled() ||
+       controller->ShouldDisplayVerticalTabs())) {
     // TODO(crbug.com/475222200): When in immersive, swapping between tab
     // strip types create duplicate tab strips. Until that is resolved, disable
     // the ability to swap between tab strips while in immersive.
diff --git a/chrome/browser/ui/tabs/BUILD.gn b/chrome/browser/ui/tabs/BUILD.gn
index 1477a4ea0b..e5deeb3bca 100644
--- a/chrome/browser/ui/tabs/BUILD.gn
+++ b/chrome/browser/ui/tabs/BUILD.gn
@@ -481,6 +481,7 @@ if (!is_android) {
     }
 
     deps = [
+      "//roamux/common",
       ":tab_group",
       ":tab_model",
       ":tab_strip",
diff --git a/chrome/browser/ui/views/frame/layout/browser_view_tabbed_layout_impl.cc b/chrome/browser/ui/views/frame/layout/browser_view_tabbed_layout_impl.cc
index 3f3758fd03..9cc30e7035 100644
--- a/chrome/browser/ui/views/frame/layout/browser_view_tabbed_layout_impl.cc
+++ b/chrome/browser/ui/views/frame/layout/browser_view_tabbed_layout_impl.cc
@@ -730,8 +730,23 @@ BrowserViewTabbedLayoutImpl::CalculateProposedLayout(
         last_vertical_tab_strip_width_ = vertical_tab_strip_width;
       }
 
+      // Roamux (roam-8/9): dock on the roamux placement's PHYSICAL side when
+      // the roamux placement drives the display (upstream pref keeps the
+      // stock leading dock). ProposedLayout coordinates are logical — they
+      // mirror under RTL — so the physical side flips here (roam-9 D1).
+      const PrefService* const roamux_prefs = browser()->profile()->GetPrefs();
+      const bool roamux_physical_right =
+          roamux::ShouldDockVerticalTabStripRight(roamux_prefs);
+      const bool roamux_physical_left =
+          roamux::ShouldDockVerticalTabStripLeft(roamux_prefs);
+      bool roamux_dock_right = false;
+      if (roamux_physical_right || roamux_physical_left) {
+        roamux_dock_right = roamux_physical_right != base::i18n::IsRTL();
+      }
       vertical_tab_strip_bounds =
-          gfx::Rect(params.visual_client_area.x(),
+          gfx::Rect(roamux_dock_right ? params.visual_client_area.right() -
+                                            vertical_tab_strip_width
+                                      : params.visual_client_area.x(),
                     params.visual_client_area.y() +
                         vertical_tab_strip_animation.top_offset,
                     vertical_tab_strip_width,
@@ -746,7 +761,7 @@ BrowserViewTabbedLayoutImpl::CalculateProposedLayout(
       if (adjust_for_cracking) {
         inset_amount -= 1;
       }
-      params.InsetHorizontal(inset_amount, /*leading=*/true);
+      params.InsetHorizontal(inset_amount, /*leading=*/!roamux_dock_right);
 
       // Let the vertical tab strip animate out over the content.
       if (vertical_tab_strip_animation.current_motion) {
@@ -755,11 +770,19 @@ BrowserViewTabbedLayoutImpl::CalculateProposedLayout(
                 TabStripAnimations::kExpand ||
             vertical_tab_strip_animation.current_motion ==
                 TabStripAnimations::kCollapse;
-        unclipped_contents_region.Inset(gfx::Insets::TLBR(
-            0, VerticalTabStripRegionView::kCollapsedWidth, 0, 0));
+        unclipped_contents_region.Inset(
+            roamux_dock_right
+                ? gfx::Insets::TLBR(
+                      0, 0, 0, VerticalTabStripRegionView::kCollapsedWidth)
+                : gfx::Insets::TLBR(
+                      0, VerticalTabStripRegionView::kCollapsedWidth, 0, 0));
       } else {
-        unclipped_contents_region.Inset(gfx::Insets::TLBR(
-            0, horizontal_layout.vertical_tab_strip_width, 0, 0));
+        unclipped_contents_region.Inset(
+            roamux_dock_right
+                ? gfx::Insets::TLBR(
+                      0, 0, 0, horizontal_layout.vertical_tab_strip_width)
+                : gfx::Insets::TLBR(
+                      0, horizontal_layout.vertical_tab_strip_width, 0, 0));
       }
     }
     layout.AddChild(views().vertical_tab_strip_region_view,
diff --git a/chrome/browser/ui/tabs/tab_menu_model.cc b/chrome/browser/ui/tabs/tab_menu_model.cc
index ce98101..3feca1e 100644
--- a/chrome/browser/ui/tabs/tab_menu_model.cc
+++ b/chrome/browser/ui/tabs/tab_menu_model.cc
@@ -24,6 +24,7 @@
 #include "chrome/browser/glic/resources/grit/glic_browser_resources.h"
 #include "chrome/browser/profiles/profile.h"
 #include "roamux/browser/ui/tabs/tab_strip_position_menu.h"
+#include "roamux/common/roamux_features.h"
 #include "chrome/browser/send_tab_to_self/send_tab_to_self_util.h"
 #include "chrome/browser/ui/browser_window/public/browser_window_features.h"
 #include "chrome/browser/ui/browser_window/public/browser_window_interface.h"
@@ -345,7 +346,14 @@ void TabMenuModel::Build(int index) {
     }
   }
 
-  if (tabs::kVerticalTabsToggleInTabContextMenu.Get() && controller) {
+  // Roamux (roam-8): only offer the toggle when it can act — upstream feature
+  // on, or a roamux-driven vertical display to switch back from.
+  if (tabs::kVerticalTabsToggleInTabContextMenu.Get() && controller &&
+      // Roamux (roam-183): the roamux placement is the sole tab-position
+      // control when the flag is on — suppress the upstream toggle.
+      !base::FeatureList::IsEnabled(roamux::features::kTabStripPosition) &&
+      (tabs::IsVerticalTabsFeatureEnabled() ||
+       controller->ShouldDisplayVerticalTabs())) {
     // TODO(crbug.com/475222200): When in immersive, swapping between tab
     // strip types create duplicate tab strips. Until that is resolved,
     // disable the ability to swap between tab strips while in immersive.
