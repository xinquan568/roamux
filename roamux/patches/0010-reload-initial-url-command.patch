diff --git a/chrome/app/chrome_command_ids.h b/chrome/app/chrome_command_ids.h
index b36306e291..affce00e7f 100644
--- a/chrome/app/chrome_command_ids.h
+++ b/chrome/app/chrome_command_ids.h
@@ -81,6 +81,8 @@
 #define IDC_TOGGLE_VERTICAL_TABS        34056
 #define IDC_VERTICAL_TABS_SEND_FEEDBACK 34057
 #define IDC_TOGGLE_VERTICAL_TABS_EXPAND_ON_HOVER 34058
+// Roamux (roam-12): reload the tab's captured initial URL.
+#define IDC_RELOAD_INITIAL_URL          34059
 
 // Web app window commands
 #define IDC_COPY_URL                    34060
diff --git a/chrome/browser/ui/browser_command_controller.cc b/chrome/browser/ui/browser_command_controller.cc
index 61e834cfb3..b39908db48 100644
--- a/chrome/browser/ui/browser_command_controller.cc
+++ b/chrome/browser/ui/browser_command_controller.cc
@@ -54,6 +54,7 @@
 #include "chrome/browser/ui/browser.h"
 #include "chrome/browser/ui/browser_actions.h"
 #include "chrome/browser/ui/browser_commands.h"
+#include "roamux/browser/tabs/reload_initial_url_command.h"
 #include "chrome/browser/ui/browser_element_identifiers.h"
 #include "chrome/browser/ui/browser_finder.h"
 #include "chrome/browser/ui/browser_window.h"
@@ -661,6 +662,10 @@ bool BrowserCommandController::ExecuteCommandWithDisposition(
     case IDC_TOGGLE_VERTICAL_TABS:
       ToggleVerticalTabs(browser_);
       break;
+    // Roamux (roam-12): reload the captured initial URL.
+    case IDC_RELOAD_INITIAL_URL:
+      roamux::tabs::ReloadInitialUrl(browser_);
+      break;
     case IDC_VERTICAL_TABS_SEND_FEEDBACK:
       chrome::ShowFeedbackPage(browser_, feedback::kFeedbackSourceVerticalTabs,
                                /*description_template=*/"",
@@ -1599,6 +1604,9 @@ void BrowserCommandController::InitCommandState() {
   command_updater_.UpdateCommandEnabled(IDC_VERTICAL_TABS_SEND_FEEDBACK, true);
   command_updater_.UpdateCommandEnabled(
       IDC_TOGGLE_VERTICAL_TABS_EXPAND_ON_HOVER, true);
+  // Roamux (roam-12): dynamic — refreshed in UpdateCommandsForTabState.
+  command_updater_.UpdateCommandEnabled(
+      IDC_RELOAD_INITIAL_URL, roamux::tabs::CanReloadInitialUrl(browser_));
 #if BUILDFLAG(IS_CHROMEOS)
   command_updater_.UpdateCommandEnabled(IDC_TOGGLE_MULTITASK_MENU, true);
   command_updater_.UpdateCommandEnabled(IDC_MINIMIZE_WINDOW, true);
@@ -1958,6 +1966,10 @@ void BrowserCommandController::UpdateCommandsForTabState() {
 
   content::WebContents* current_web_contents =
       browser_->tab_strip_model()->GetActiveWebContents();
+
+  // Roamux (roam-12): the reload-initial-url command follows the active tab.
+  command_updater_.UpdateCommandEnabled(
+      IDC_RELOAD_INITIAL_URL, roamux::tabs::CanReloadInitialUrl(browser_));
   if (!current_web_contents) {  // May be NULL during tab restore.
     return;
   }
diff --git a/chrome/browser/ui/cocoa/accelerators_cocoa.mm b/chrome/browser/ui/cocoa/accelerators_cocoa.mm
index 3c40f982b3..f3bf6526f1 100644
--- a/chrome/browser/ui/cocoa/accelerators_cocoa.mm
+++ b/chrome/browser/ui/cocoa/accelerators_cocoa.mm
@@ -95,6 +95,11 @@ const struct AcceleratorMapping {
     {IDC_RELOAD, ui::EF_COMMAND_DOWN, ui::VKEY_R},
     {IDC_RELOAD_BYPASSING_CACHE, ui::EF_COMMAND_DOWN | ui::EF_SHIFT_DOWN,
      ui::VKEY_R},
+    // Roamux (roam-12): reload the captured initial URL. Collision audit
+    // 2026-07-08: Ctrl+Cmd chords in this table are NEW_TAB_GROUP and the
+    // fullscreen VKEY_F chord; no VKEY_R conflict. Re-check on uprev.
+    {IDC_RELOAD_INITIAL_URL, ui::EF_COMMAND_DOWN | ui::EF_CONTROL_DOWN,
+     ui::VKEY_R},
     {IDC_ZOOM_NORMAL, ui::EF_COMMAND_DOWN, ui::VKEY_0},
     {IDC_HOME, ui::EF_COMMAND_DOWN | ui::EF_SHIFT_DOWN, ui::VKEY_H},
     {IDC_BACK, ui::EF_COMMAND_DOWN, ui::VKEY_OEM_4},
diff --git a/chrome/browser/ui/BUILD.gn b/chrome/browser/ui/BUILD.gn
index caced0b..e1f3712 100644
--- a/chrome/browser/ui/BUILD.gn
+++ b/chrome/browser/ui/BUILD.gn
@@ -5699,6 +5699,13 @@ static_library("ui") {
     allow_circular_includes_from +=
         [ "//chrome/browser/ui/webui/tab_strip_internals:impl" ]
   }
+
+  # Roamux (roam-12): the reload-initial-url command behavior (needs
+  # Browser*; roamux-owned files compiled into this target, 0009-precedent).
+  sources += [
+    "//roamux/browser/tabs/reload_initial_url_command.cc",
+    "//roamux/browser/tabs/reload_initial_url_command.h",
+  ]
 }
 
 # These are the dependencies for the "ui" target that are outside of
