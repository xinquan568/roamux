diff --git a/chrome/browser/BUILD.gn b/chrome/browser/BUILD.gn
index 63c2601cfc..9eeb9da6c2 100644
--- a/chrome/browser/BUILD.gn
+++ b/chrome/browser/BUILD.gn
@@ -4195,6 +4195,11 @@ static_library("browser") {
       "fullscreen_mac.mm",
       "global_keyboard_shortcuts_mac.h",
       "global_keyboard_shortcuts_mac.mm",
+      # Roamux (roam-13): the shared shortcut surface (mac-only product).
+      "//roamux/browser/tabs/shortcut_registry_mac.h",
+      "//roamux/browser/tabs/shortcut_registry_mac.mm",
+      "//roamux/browser/ui/webui/roamux_shortcuts_handler.cc",
+      "//roamux/browser/ui/webui/roamux_shortcuts_handler.h",
       "icon_loader_mac.mm",
       "memory_details_mac.cc",
       "platform_util_mac.mm",
diff --git a/chrome/browser/global_keyboard_shortcuts_mac.mm b/chrome/browser/global_keyboard_shortcuts_mac.mm
index bc8c1e5d95..4e3695179f 100644
--- a/chrome/browser/global_keyboard_shortcuts_mac.mm
+++ b/chrome/browser/global_keyboard_shortcuts_mac.mm
@@ -4,6 +4,8 @@
 
 #include "chrome/browser/global_keyboard_shortcuts_mac.h"
 
+#include "roamux/browser/tabs/shortcut_registry_mac.h"
+
 #import <AppKit/AppKit.h>
 #include <Carbon/Carbon.h>
 
@@ -214,6 +216,12 @@ CommandForKeyEventResult CommandForKeyEvent(NSEvent* event) {
   if (cmdNum != NO_COMMAND)
     return MainMenuCommand(cmdNum);
 
+  // Roamux (roam-13): pref-overridable Roamux shortcuts (per-entry
+  // flag-gated; roam-12's default chord dispatches through here too).
+  int roamuxCmd = roamux::tabs::CommandForKeyEventOverride(event);
+  if (roamuxCmd >= 0)
+    return ShortcutCommand(roamuxCmd);
+
   // Scan through keycodes and see if it corresponds to one of the non-menu
   // shortcuts.
   for (NSMenuItem* menu_item : GetMenuItemsNotPresentInMainMenu()) {
diff --git a/chrome/browser/ui/webui/settings/settings_ui.cc b/chrome/browser/ui/webui/settings/settings_ui.cc
index ecb360f352..c1046d7e83 100644
--- a/chrome/browser/ui/webui/settings/settings_ui.cc
+++ b/chrome/browser/ui/webui/settings/settings_ui.cc
@@ -62,6 +62,9 @@
 #include "chrome/browser/ui/webui/settings/about_handler.h"
 #include "chrome/browser/ui/webui/settings/accessibility_main_handler.h"
 #include "chrome/browser/ui/webui/settings/appearance_handler.h"
+#if BUILDFLAG(IS_MAC)
+#include "roamux/browser/ui/webui/roamux_shortcuts_handler.h"
+#endif
 #include "chrome/browser/ui/webui/settings/browser_lifetime_handler.h"
 #include "chrome/browser/ui/webui/settings/downloads_handler.h"
 #include "chrome/browser/ui/webui/settings/font_handler.h"
@@ -234,6 +237,10 @@ SettingsUI::SettingsUI(content::WebUI* web_ui)
       "worker-src blob: chrome://resources 'self';");
 
   AddSettingsPageUIHandler(std::make_unique<AppearanceHandler>(web_ui));
+#if BUILDFLAG(IS_MAC)
+  // Roamux (roam-13): the shared shortcut surface (§4.3).
+  AddSettingsPageUIHandler(std::make_unique<roamux::RoamuxShortcutsHandler>());
+#endif
 
 #if BUILDFLAG(IS_CHROMEOS)
   AddSettingsPageUIHandler(std::make_unique<LanguagesHandler>(profile));
diff --git a/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc b/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc
index e77aea98dd..c622d2d59b 100644
--- a/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc
+++ b/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc
@@ -52,6 +52,9 @@
 #include "chrome/browser/ui/webui/management/management_ui.h"
 #include "chrome/browser/ui/webui/policy_indicator_localized_strings_provider.h"
 #include "chrome/browser/ui/webui/settings/glic_handler.h"
+#if BUILDFLAG(IS_MAC)
+#include "roamux/browser/tabs/shortcut_registry.h"
+#endif
 #include "chrome/browser/ui/webui/settings/reset_settings_handler.h"
 #include "chrome/browser/ui/webui/settings/shared_settings_localized_strings_provider.h"
 #include "chrome/browser/ui/webui/version/version_ui.h"
@@ -592,6 +595,15 @@ void AddAppearanceStrings(content::WebUIDataSource* html_source,
       "showTabSearchEnabled",
       tabs::IsVerticalTabsFeatureEnabled() ||
           base::FeatureList::IsEnabled(tabs::kHorizontalTabStripComboButton));
+#if BUILDFLAG(IS_MAC)
+  // Roamux (roam-13): the shared shortcut surface shows when any entry's
+  // feature is enabled.
+  html_source->AddBoolean(
+      "roamuxShortcutsEnabled",
+      !roamux::tabs::EnabledShortcuts(roamux::tabs::AllShortcuts()).empty());
+#else
+  html_source->AddBoolean("roamuxShortcutsEnabled", false);
+#endif
   html_source->AddBoolean("showProjectsPanelEnabled",
                           tab_groups::IsProjectsPanelFeatureEnabled());
   html_source->AddBoolean(
diff --git a/chrome/browser/resources/settings/appearance_page/appearance_page.html b/chrome/browser/resources/settings/appearance_page/appearance_page.html
index 5e0a09805b..239128e96e 100644
--- a/chrome/browser/resources/settings/appearance_page/appearance_page.html
+++ b/chrome/browser/resources/settings/appearance_page/appearance_page.html
@@ -237,6 +237,32 @@
           </settings-toggle-button>
         </template>
 
+        <template is="dom-if" if="[[roamuxShortcutsEnabled_]]">
+          <div class="cr-row">
+            <div class="flex cr-padded-text" aria-hidden="true">
+              Keyboard shortcuts (Roamux)
+            </div>
+          </div>
+          <div class="list-frame">
+            <template is="dom-repeat" items="[[roamuxShortcuts_]]">
+              <div class="list-item">
+                <div class="flex">[[item.label]]</div>
+                <div class="cr-padded-text">[[item.chordText]]</div>
+                <cr-button on-click="onRoamuxRebindClick_"
+                    data-key$="[[item.key]]">
+                  Change
+                </cr-button>
+              </div>
+            </template>
+            <div class="list-item" hidden$="[[!roamuxRecordingKey_]]">
+              <div class="flex cr-padded-text">
+                Press the new shortcut… (Esc cancels)
+              </div>
+              <div class="cr-padded-text">[[roamuxRebindStatus_]]</div>
+            </div>
+          </div>
+        </template>
+
         <settings-toggle-button class="hr" id="showSavedTabGroups"
             pref="{{prefs.bookmark_bar.show_tab_groups}}"
             label="$i18n{showTabGroupsInBookmarksBar}"
diff --git a/chrome/browser/resources/settings/appearance_page/appearance_page.ts b/chrome/browser/resources/settings/appearance_page/appearance_page.ts
index 3c0e5f0ca3..ea3c4f5767 100644
--- a/chrome/browser/resources/settings/appearance_page/appearance_page.ts
+++ b/chrome/browser/resources/settings/appearance_page/appearance_page.ts
@@ -27,6 +27,7 @@ import {PolymerElement} from 'chrome://resources/polymer/v3_0/polymer/polymer_bu
 
 import type {DropdownMenuOptionList, SettingsDropdownMenuElement} from '../controls/settings_dropdown_menu.js';
 import type {SettingsToggleButtonElement} from '../controls/settings_toggle_button.js';
+import {sendWithPromise} from 'chrome://resources/js/cr.js';
 import {loadTimeData} from '../i18n_setup.js';
 import type {MetricsBrowserProxy} from '../metrics_browser_proxy.js';
 import {MetricsBrowserProxyImpl} from '../metrics_browser_proxy.js';
@@ -268,6 +269,30 @@ export class SettingsAppearancePageElement extends
         },
       },
 
+      roamuxShortcutsEnabled_: {
+        type: Boolean,
+        value() {
+          return loadTimeData.getBoolean('roamuxShortcutsEnabled');
+        },
+      },
+
+      roamuxShortcuts_: {
+        type: Array,
+        value() {
+          return [];
+        },
+      },
+
+      roamuxRecordingKey_: {
+        type: String,
+        value: '',
+      },
+
+      roamuxRebindStatus_: {
+        type: String,
+        value: '',
+      },
+
       showProjectsPanelEnabled_: {
         type: Boolean,
         value() {
@@ -313,6 +338,12 @@ export class SettingsAppearancePageElement extends
       Array<{value: ColorSchemeMode, name: string}>;
   declare private selectedColorSchemeMode_: ColorSchemeMode|undefined;
   declare private pageZoomLevels_: number[];
+  declare private roamuxShortcutsEnabled_: boolean;
+  declare private roamuxShortcuts_:
+      Array<{key: string, label: string, chordText: string}>;
+  declare private roamuxRecordingKey_: string;
+  declare private roamuxRebindStatus_: string;
+  private roamuxKeydownListener_: ((e: KeyboardEvent) => void)|null = null;
   declare private themeSublabel_: string;
   declare private themeUrl_: string;
   declare private systemTheme_: SystemTheme;
@@ -365,6 +396,59 @@ export class SettingsAppearancePageElement extends
                       ?.value;
             });
     this.colorSchemeModeHandler_.initializeColorSchemeMode();
+
+    if (this.roamuxShortcutsEnabled_) {
+      this.roamuxRefreshShortcuts_();
+    }
+  }
+
+  private roamuxRefreshShortcuts_() {
+    sendWithPromise('roamuxGetShortcuts').then((list: unknown) => {
+      this.roamuxShortcuts_ =
+          list as Array<{key: string, label: string, chordText: string}>;
+    });
+  }
+
+  private onRoamuxRebindClick_(e: Event) {
+    const key = (e.currentTarget as HTMLElement).dataset['key']!;
+    this.roamuxRecordingKey_ = key;
+    this.roamuxRebindStatus_ = '';
+    this.roamuxKeydownListener_ = (event: KeyboardEvent) => {
+      event.preventDefault();
+      event.stopPropagation();
+      if (event.key === 'Escape') {
+        this.roamuxStopRecording_();
+        return;
+      }
+      if (['Shift', 'Control', 'Alt', 'Meta'].includes(event.key)) {
+        return;
+      }
+      const chord = {
+        cmd: event.metaKey,
+        shift: event.shiftKey,
+        ctrl: event.ctrlKey,
+        opt: event.altKey,
+        code: event.code,
+      };
+      sendWithPromise('roamuxRebindShortcut', this.roamuxRecordingKey_, chord)
+          .then((result: unknown) => {
+            const error = result as string;
+            this.roamuxRebindStatus_ =
+                error === '' ? '' : `Rejected (${error}) — try another chord`;
+            this.roamuxStopRecording_();
+            this.roamuxRefreshShortcuts_();
+          });
+    };
+    document.addEventListener('keydown', this.roamuxKeydownListener_, true);
+  }
+
+  private roamuxStopRecording_() {
+    if (this.roamuxKeydownListener_) {
+      document.removeEventListener('keydown', this.roamuxKeydownListener_,
+                                   true);
+      this.roamuxKeydownListener_ = null;
+    }
+    this.roamuxRecordingKey_ = '';
   }
 
   override disconnectedCallback() {
