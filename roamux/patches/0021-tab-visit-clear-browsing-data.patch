diff --git a/chrome/browser/browsing_data/BUILD.gn b/chrome/browser/browsing_data/BUILD.gn
index c13b306f00..7778887961 100644
--- a/chrome/browser/browsing_data/BUILD.gn
+++ b/chrome/browser/browsing_data/BUILD.gn
@@ -169,6 +169,11 @@ source_set("impl") {
     "//components/url_matcher",
     "//components/web_cache/browser",
     "//media/mojo/services",
+
+    # Roamux (roam-28 / I-4.8): the low tab-visit hook registry so the delegate
+    # can clear the settled-visit journal without a browsing_data -> ui/tabs
+    # cycle (the impl is registered from the ui-layer journal factory ctor).
+    "//roamux/browser/tab_visit",
     "//ui/strings:ui_strings__strings_grit",
   ]
 
diff --git a/chrome/browser/browsing_data/chrome_browsing_data_remover_delegate.cc b/chrome/browser/browsing_data/chrome_browsing_data_remover_delegate.cc
index 4e140e65c8..8cff41a720 100644
--- a/chrome/browser/browsing_data/chrome_browsing_data_remover_delegate.cc
+++ b/chrome/browser/browsing_data/chrome_browsing_data_remover_delegate.cc
@@ -165,6 +165,7 @@
 #include "net/base/registry_controlled_domains/registry_controlled_domain.h"
 #include "net/http/http_transaction_factory.h"
 #include "net/net_buildflags.h"
+#include "roamux/browser/tab_visit/settled_visit_clear_hook.h"
 #include "services/network/public/mojom/clear_data_filter.mojom.h"
 #include "third_party/perfetto/include/perfetto/tracing/track.h"
 
@@ -410,6 +411,17 @@ void ChromeBrowsingDataRemoverDelegate::RemoveEmbedderData(
   //////////////////////////////////////////////////////////////////////////////
   // DATA_TYPE_HISTORY
   if ((remove_mask & constants::DATA_TYPE_HISTORY) && may_delete_history) {
+    // Roamux (roam-28 / I-4.8): clear the settled-visit journal + orphaned
+    // closed tab_state for the SAME [delete_begin_, delete_end_) range, and
+    // purge the traversal coordinator's in-memory reopenable cache. Invoked via
+    // the low //roamux/browser/tab_visit hook registry because the per-profile
+    // journal factory + coordinator live in //chrome/browser/ui/tabs (a higher
+    // GN layer); the completion token defers "done" until the async store clear
+    // finishes. Gated by may_delete_history so enterprise policy is respected.
+    roamux::tab_visit::RunClearJournalHook(
+        profile_, delete_begin_, delete_end_,
+        CreateTaskCompletionClosure(TracingDataType::kRoamuxVisitJournal));
+
     history::HistoryService* history_service =
         HistoryServiceFactory::GetForProfile(
             profile_, ServiceAccessType::EXPLICIT_ACCESS);
@@ -1564,6 +1576,9 @@ const char* ChromeBrowsingDataRemoverDelegate::GetHistogramSuffix(
       return "Synchronous";
     case TracingDataType::kHistory:
       return "History";
+    // Roamux (roam-28 / I-4.8).
+    case TracingDataType::kRoamuxVisitJournal:
+      return "RoamuxVisitJournal";
     case TracingDataType::kAutofillData:
       return "AutofillData";
     case TracingDataType::kAutofillOrigins:
diff --git a/chrome/browser/browsing_data/chrome_browsing_data_remover_delegate.h b/chrome/browser/browsing_data/chrome_browsing_data_remover_delegate.h
index 08d26d7a25..5cd12198b6 100644
--- a/chrome/browser/browsing_data/chrome_browsing_data_remover_delegate.h
+++ b/chrome/browser/browsing_data/chrome_browsing_data_remover_delegate.h
@@ -147,7 +147,11 @@ class ChromeBrowsingDataRemoverDelegate
     // See also kDisableAutoSigninForProfilePasswords.
     kDisableAutoSigninForAccountPasswords = 46,
 
-    kMaxValue = kDisableAutoSigninForAccountPasswords,
+    // Roamux (roam-28 / I-4.8): the settled-visit journal + closed-tab-state
+    // clear task (see //roamux/browser/tab_visit).
+    kRoamuxVisitJournal = 47,
+
+    kMaxValue = kRoamuxVisitJournal,
   };
   // LINT.ThenChange(//tools/metrics/histograms/metadata/history/enums.xml:ChromeBrowsingDataRemoverTasks)
 
diff --git a/tools/metrics/histograms/metadata/history/enums.xml b/tools/metrics/histograms/metadata/history/enums.xml
index f07f57d920..7437fd9d1c 100644
--- a/tools/metrics/histograms/metadata/history/enums.xml
+++ b/tools/metrics/histograms/metadata/history/enums.xml
@@ -125,6 +125,7 @@ chromium-metrics-reviews@google.com.
   <int value="44" label="kWebrtcVideoPerfHistory"/>
   <int value="45" label="kMediaDeviceSalts"/>
   <int value="46" label="kDisableAutoSigninForAccountPasswords"/>
+  <int value="47" label="kRoamuxVisitJournal"/>
 </enum>
 
 <!-- LINT.ThenChange(//chrome/browser/browsing_data/chrome_browsing_data_remover_delegate.h:TracingDataType) -->
diff --git a/tools/metrics/histograms/metadata/history/histograms.xml b/tools/metrics/histograms/metadata/history/histograms.xml
index e87a419a14..9051346fd4 100644
--- a/tools/metrics/histograms/metadata/history/histograms.xml
+++ b/tools/metrics/histograms/metadata/history/histograms.xml
@@ -174,6 +174,7 @@ chromium-metrics-reviews@google.com.
     <variant name="Passwords" summary="Passwords"/>
     <variant name="PasswordsStatistics" summary="PasswordsStatistics"/>
     <variant name="ReportingCache" summary="ReportingCache"/>
+    <variant name="RoamuxVisitJournal" summary="RoamuxVisitJournal"/>
     <variant name="SecurePaymentConfirmationCredentials"
         summary="SecurePaymentConfirmationCredentials"/>
     <variant name="Synchronous" summary="Synchronous"/>
