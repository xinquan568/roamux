diff --git a/chrome/browser/ui/toolbar/BUILD.gn b/chrome/browser/ui/toolbar/BUILD.gn
index 49b5365919..36e465d3d8 100644
--- a/chrome/browser/ui/toolbar/BUILD.gn
+++ b/chrome/browser/ui/toolbar/BUILD.gn
@@ -77,6 +77,7 @@ source_set("toolbar") {
       "//chrome/browser/ui/actions:actions_headers",
       "//chrome/browser/ui/tabs:tab_strip",
       "//components/user_education/common",
+      "//roamux/browser/ui/icons",
     ]
   }
 
diff --git a/chrome/browser/ui/toolbar/chrome_location_bar_model_delegate.cc b/chrome/browser/ui/toolbar/chrome_location_bar_model_delegate.cc
index 33f582e1c7..3e0dfac030 100644
--- a/chrome/browser/ui/toolbar/chrome_location_bar_model_delegate.cc
+++ b/chrome/browser/ui/toolbar/chrome_location_bar_model_delegate.cc
@@ -42,6 +42,7 @@
 #if !BUILDFLAG(IS_ANDROID)
 #include "components/omnibox/browser/vector_icons.h"  // nogncheck
 #include "components/vector_icons/vector_icons.h"     // nogncheck
+#include "roamux/browser/ui/icons/vector_icons.h"     // nogncheck
 #endif
 
 #if BUILDFLAG(ENABLE_OFFLINE_PAGES)
@@ -190,7 +191,9 @@ const gfx::VectorIcon* ChromeLocationBarModelDelegate::GetVectorIconOverride()
   }
 
   if (url.SchemeIs(content::kChromeUIScheme)) {
-    return &omnibox::kProductChromeRefreshIcon;
+    // Roamux (roam-159): the chrome:// chip shows the fixed-colour Roamux
+    // mark (PATH_COLOR_ARGB overrides the security-chip tint).
+    return &roamux::kRoamuxProductIcon;
   }
 
   if (url.SchemeIs(extensions::kExtensionScheme)) {
