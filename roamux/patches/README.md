<!-- SPDX-License-Identifier: Apache-2.0 -->
# `roamux/patches` — minimal upstream patches (roam-2 / plan §12.2)

In-function hook points and generated-table edits that **neither** an additive `//roamux` file **nor** a
`chromium_src` override can reach — e.g. a one-line `ROAMUX_*` macro insert, an `IDC_*` command-id enum
entry, a macOS accelerator table row, `RegisterProfilePrefs`, or a `BrowsingDataRemover` registration.

## The runhook (roam-2)

```sh
python3 roamux/build/apply_patches.py --chromium-src ~/chromium/src            # apply (idempotent)
python3 roamux/build/apply_patches.py --chromium-src ~/chromium/src --check   # verify, no mutation
```

Patches apply in **name order** (`NNNN-slug.patch`, `git diff` format). Already-applied patches are
skipped (`[applied]`); one that neither is applied nor applies cleanly **fails loudly naming the patch**
(§12.5 — the rebase signal). §12.4's target state invokes this at `gclient sync` time; until then it
runs manually / from the build gate.

## Current inventory

| Patch | Kind | What it does |
|---|---|---|
| `0001-gn-all-add-roamux-targets.patch` | **persistent** | wires `//roamux` + the `//roamux:roamux_tests` group into `gn_all` (§12.4) — new test targets join the group, never this patch |
| `0002-chromium-src-include-redirect.patch` | **persistent** | enables `chromium_src` shadowing (one `include_dirs` line, §12.2 mechanism 2) |
| `0003-sample-marker.patch` | **sample** | one-line inert marker proving the runhook end-to-end (roam-2 test) |
| `0004-register-profile-prefs.patch` | **persistent** | the §12.2 registrar hook (roam-3): `roamux::prefs::RegisterProfilePrefs` call + include in `browser_prefs.cc`, plus the `//roamux/common` dep edge on its owning GN target. **roam-182**: also calls `roamux::prefs::MigrateProfilePrefs` from `MigrateObsoleteProfilePrefs` (one-time startup normalization of `vertical_tabs.enabled` onto the roamux placement) |
| `0005-tab-menu-tab-strip-position.patch` | **persistent** | the §12.2 tab-strip context-menu hook (roam-6): flag-gated "Tab strip position (Roamux)" submenu (member + `Build()` call in `TabMenuModel`) plus the `//roamux/browser/ui/tabs` dep edge; roam-181 widens the touch set to `tab/tab_context_menu_controller.cc` (range guards so roamux command ids never reach the upstream `ContextMenuCommand` cast); **roam-194** widens those guards to also cover the Initial-URL submenu ids (2110–2112, predicate `IsInitialUrlCommandId` from `initial_url_menu.h`). **roam-184**: the submenu title is end-user copy "Tab strip position" (no "(Roamux)"), set in `//roamux` source (`tab_strip_position_menu.cc`), not this patch |
| `0006-settings-appearance-tab-strip-position.patch` | **persistent** | the settings WebUI insertion (roam-6; **maintainer-authorized §12.2 mechanism revision**, see issue #6 — at pin M149 the `chromium_src` include-redirect cannot reach `build_webui()` resource lists, so the insertion lands as this minimal patch): Appearance-page row + `settings_private` allowlist entry + `roamuxTabStripPositionEnabled` loadTimeData, plus the `//roamux/common` dep edge on `//chrome/browser/ui`; **roam-183**: the upstream `#tabStripPosition` row is hidden when the roamux flag is on by gating the `showVerticalTabsEnabled_` property's `value()` (its only consumer), leaving the Appearance HTML pristine so patch 0024's context is untouched; **roam-184**: end-user row label ("Tab strip position", no "(Roamux)") + Left/Right sub-toggles (expand-on-hover / organizer) re-homing the upstream vertical-tabs sub-settings |
| `0007-browser-layout-tab-strip-placement.patch` | **persistent** | the §12.2 tab-strip-region placement hook (roam-7), at the pin's reworked coordinates (`BrowserViewTabbedLayoutImpl::CalculateProposedLayout`, not the plan's pre-rework `BrowserViewLayout::Layout()`): flag-gated bottom-band docking via `roamux::ComputeBottomStripLayout`, the `BrowserView` live-switch observer member + instantiation, and the `//roamux/browser/ui/tabs` dep edge on `//chrome/browser/ui`. Upstream vertical tabs win structurally (coexistence rule D1, tested) |
| `0008-vertical-tab-strip-roamux-placement.patch` | **persistent** | vertical-strip reuse (roam-8; **maintainer-authorized issue-surface revision**, see issue #8 — upstream M149 ships the vertical strip, so Roamux maps placement left/right onto it instead of building `RoamuxVerticalTabStrip`): controller creation-gate OR (`roamux::kTabStripPosition`), display-gate OR (`ShouldDisplayVerticalTabsForPlacement`), placement-pref observer with a display-flip guard (no spurious strip re-init), right-dock bounds mirror in the tabbed layout, plus `//roamux/common` dep edges. **Amended by roam-9**: the roamux-driven dock side is physical/RTL-invariant (ProposedLayout coordinates are logical, so the side flips under RTL). **Amended by roam-182 (sole authority; stance revision — Roamux now WRITES the upstream pref, maintainer-authorized 2026-07-20)**: with the flag on, `ShouldDisplayVerticalTabs` and the dock predicates follow the roamux placement ALONE (upstream-pref contribution/precedence removed — it made every placement a no-op when upstream vertical tabs were on), and `SetVerticalTabsEnabled` maps the upstream toggle onto placement (vertical→Left, horizontal→Top). **roam-183**: the three upstream vertical-tabs menu toggles (tab context / app Tools / system menu) are suppressed when the roamux flag is on |
| `0009-tab-uid-hooks.patch` | **persistent** | the durable-tab-uid hooks (roam-10, the §12.2 session-state row at pin coordinates): per-tab helper creation in `TabFeatures::Init`; restore hand-off + closed-tab capture beside the glic extra-data precedents (`browser_tabrestore.cc`, `browser_live_tab_context.cc`); `TabUidServiceFactory` registration in `EnsureBrowserContextKeyedServiceFactoriesBuilt`; `sources +=`/dep edge on the tab target (chrome-facing helper/factory are roamux-owned files compiled into the upstream target — no GN cycle). **Amended by roam-11**: the same attach/discard hunks also create/transfer the initial-url capture helper, and the sources list gains it |
| `0010-reload-initial-url-command.patch` | **persistent** | the §12.2 command rows (roam-12): `IDC_RELOAD_INITIAL_URL` (34059), `BrowserCommandController` execute + enabled-state (init + tab-state refresh), the macOS `Ctrl+Cmd+R` accelerator row (collision audit 2026-07-08: no Ctrl+Cmd `VKEY_R` conflict — re-check on uprev), and the `//chrome/browser/ui` `sources +=` for the additive command pair |
| `0011-shared-shortcut-settings.patch` | **persistent** | the §4.3 shared shortcut surface (roam-13): the mac key-event dispatch hook in `CommandForKeyEvent` (pref-overridable Roamux bindings — this is where roam-12's default chord actually dispatches), the settings handler registration (`RoamuxShortcutsHandler`, the §12.2-declared additive handler), the `roamuxShortcutsEnabled` loadTimeData + Appearance-page panel/recorder (roam-6 mechanism), and the `chrome/browser` mac `sources +=` for the roamux-owned registry-mac/handler files |
| `0009-tab-uid-hooks.patch` | *(amended, roam-14)* | sibling `TabInitialUrlHelper::PopulateExtraData` (close-time) and `SetPendingRestoredInitialUrl` (AddRestoredTab: reopen AND session restore) calls beside the uid ones — the initial URL rides roam-10's extra-data channels |
| `0012-initial-url-edit-ui.patch` | **persistent** | the §4.5 edit surface (roam-14): the `DuplicateTabAt` inherit-value+lock hook (§4.2), the tab-menu `MaybeAppendInitialUrlSubMenu` (own delegate; "Edit initial URL…" dialog + "Set initial URL to current page"), and the `//chrome/browser/ui/tabs` `sources +=` for the menu/dialog files |

| `0013-edge-chromium-importer.patch` | **persistent** | the ImporterList family (roam-15): `TYPE_EDGE_CHROMIUM` + `VISIT_SOURCE_EDGE_IMPORTED`, the IPC ImporterType range raise (both maxes), the `#if IS_MAC` `CreateImporterByType` case, the flag-gated macOS `DetectRoamuxEdgeProfiles` (calls the pure `roamux::DetectEdgeSourceProfile`), the `in_process_importer_bridge` VisitSource map, the UMA bucket + `enums.xml` value 8, the `sql/histograms.xml` DatabaseTag, and the `chrome/utility` + `chrome/browser/importer` + `chrome/test` BUILD wiring |

| `0014-edge-secrets-keychain-oscrypt.patch` | **persistent** | the §5.2 secrets surface (roam-16): a `roamux::MakeCryptoPassKey()` friend in `crypto/subtle_passkey.h` (for the Edge-key PBKDF2), `ProfileWriter::AddCookies` (plaintext CanonicalCookies → CookieManager, re-encrypting under Roamux), and the test BUILD wiring (`chrome/browser/importer` secret-stage source_set + its `chrome/test` registration) |
| `0015-tab-visit-journal-factory.patch` | **persistent** | the E4 settled-visit journal (roam-21): `SettledVisitJournalFactory` registration in `EnsureBrowserContextKeyedServiceFactoriesBuilt`; `sources +=`/dep edge on the tab target (the chrome-facing factory is a roamux-owned file compiled into the upstream target — no GN cycle; the pure service + SQLite store live in `//roamux/browser/tab_visit`); and the `sql/histograms.xml` `RoamuxSettledVisitJournal` DatabaseTag variant for the store |
| `0016-tab-visit-observer-bridge.patch` | **persistent** | the E4 observer bridge (roam-23): eager `TabVisitObserverBridgeFactory::GetInstance()` registration in `EnsureBrowserContextKeyedServiceFactoriesBuilt` so the commit-on-settle observer exists per profile, plus the `chrome/browser/ui/tabs` BUILD wiring (the bridge + need-commit predicate live in `//roamux/browser/tab_visit`) |
| `0017-tab-visit-command-ids.patch` | **persistent** | the E4 command ids (roam-25): `IDC_TAB_VISIT_BACK` (33010) / `IDC_TAB_VISIT_FORWARD` (33011) in `chrome_command_ids.h`, kept in sync with `roamux/browser/tabs/shortcut_registry.cc` |
| `0018-tab-visit-commands.patch` | **persistent** | the E4 Back/Forward dispatch (roam-25): `BrowserCommandController` execute + enabled-state hooks calling `roamux::tab_visit::TabVisit{Back,Forward}`, plus the `chrome/browser/ui` `sources +=` for the roamux-owned command pair (no dep edge) |
| `0019-tab-visit-traversal-coordinator.patch` | **persistent** | the E4 per-profile traversal coordinator (roam-25): coordinator + factory (roamux-owned files) compiled into the upstream tabs target via `sources +=`, with the factory registered in `EnsureBrowserContextKeyedServiceFactoriesBuilt` (the pure traversal controller lives in `//roamux/browser/tab_visit`) |
| `0020-tab-visit-gesture-watcher.patch` | **persistent** | the E4 per-window traversal-gesture settle watcher (roam-25): `TabVisitGestureWatcher` member + creation in `BrowserWindowFeatures::Init` (`browser_window_features.{h,cc}`) plus the `//roamux/browser/tab_visit` dep edge on the internal browser-window target |
| `0021-tab-visit-clear-browsing-data.patch` | **persistent** | the E4 clear-browsing-data integration (roam-28 / I-4.8): `ChromeBrowsingDataRemoverDelegate` clears the settled-visit journal + orphaned sidecar rows via `roamux/browser/tab_visit/settled_visit_clear_hook` (never live-tab liveness), plus the `//roamux/browser/tab_visit` dep on `chrome/browser/browsing_data` and the `kRoamuxVisitJournal`/`RoamuxVisitJournal` rows in `tools/metrics/histograms/metadata/history` `enums.xml`/`histograms.xml` |
| `0022-brave-style-profiles-name-only-creation.patch` | **persistent** | the E5 name-only creation hook (roam-29): `profile_picker_ui.cc` `AddFlags` wraps `signInProfileCreationFlowSupported` in `roamux::profiles::AllowSigninProfileCreationStep(...)` (flag-on → false → the picker WebUI's `computeStep` resolves NEW_PROFILE straight to the local name+avatar/theme path; flag-off → pure pass-through), plus the `//roamux/browser/profiles` dep edge on `source_set("profile_impl")` |
| `0023-signin-surface-suppression-on-next-startup.patch` | **persistent** | the E5 default surface suppression (roam-30): the AccountConsistencyModeManager startup derivation reads `roamux::signin::IsSigninAllowedOnNextStartup(prefs)` in place of the raw `kSigninAllowedOnNextStartup` read (flag on → `roamux.signin.optional_entry_point_enabled` AND upstream, default off ⇒ `kSigninAllowed` derives false and every §7.3 surface suppresses; flag off → pure pass-through), plus the `//roamux/browser/signin` dep edge on `source_set("impl")` |
| `0024-signin-opt-in-flags-settings-inert-gate.patch` | **persistent** | the E5 opt-in + three-state honesty (roam-31): eight flag-gated early-return gates on the `signin_ui_util` initiation funnel (delegating to `roamux::signin_ui::MaybeInterceptInertSignin` — keyless/keyed-unauthorized builds show the explanation dialog, never a Gaia tab); the `chrome://flags` entry + `flag-metadata.json` row for `--roamux-signin-opt-in` (the roamux://flags alias is owed by the branding epic); the `settings_private` allowlist entry + Appearance-page toggle row + `roamuxSigninOptInEnabled` loadTimeData for the opt-in pref; `sources +=` for the roamux-owned dialog shim in `//chrome/browser/signin:impl` + the `//components/constrained_window` dep edge. **roam-184**: appearance sections context-refreshed (the roam-184 sub-toggle block shifts 0024's leading context; content unchanged) |
| `0025-sparkle-bundle-integration.patch` | **persistent** | the E6 Sparkle bundle integration (roam-32; **menu item retired by roam-140**): flag-gated (`roamux_enable_sparkle`) merge of `//roamux/app/sparkle-Info.plist` into `chrome_app_plist` (via `info_plists`) + the `//roamux/app` bundle-copy dep on `mac_app_bundle("chrome_app")` (Sparkle.framework → Contents/Frameworks); the `InitSparkleUpdater()` call in `app_controller_mac.mm applicationDidFinishLaunching:` (now defers to the single shared `SparkleOwner`); and the `//roamux/app:app_buildflags`/`:sparkle_updater` GN dep edge on `//chrome/browser`. The "Check for Updates" menu item + `roamuxCheckForUpdates:` selector were removed by roam-140 (moved to `chrome://settings/help`). All hunks guarded by `BUILDFLAG(ROAMUX_ENABLE_SPARKLE)`; flag-off is byte-identical |
| `0026-update-service-factory-registration.patch` | **persistent** | the E6 update service (roam-85; **de-inlined by roam-140**): flag-gated (`roamux_enable_sparkle`) `RoamuxUpdateServiceFactory::GetInstance()` registration in `EnsureBrowserContextKeyedServiceFactoriesBuilt`, plus a `profiles_extra_parts_impl` `deps += [ "//roamux/browser/updates:update_service" ]` edge (the roamux-owned ObjC++ service/factory now live in that additive GN target rather than inlined `sources +=`) + the `//roamux/app:app_buildflags` edge — the per-profile facade over one process-wide Sparkle owner |
| `0028-roamux-scheme-alias.patch` | **persistent** | the E0 roamux:// scheme alias (roam-91, rebranded by roam-93): registers `"roamux"` as a standard scheme in `ChromeContentClient::AddAdditionalSchemes` (annotated literal, parse-only); lists `"roamux"` in `ProfileIOData::IsHandledProtocol` so renderers cannot request it (`CanRequestURL` tail — the same layer that blocks web-content `chrome://` links) and unrewritten roamux:// URLs are a no-commit dead-end (dropped) instead of reaching the OS external-protocol path; registers `roamux::MaybeRewriteRoamuxAliasURL` (flag-gated `kRoamuxSchemeAlias`, rewrite-then-return-false) in `BrowserURLHandlerCreated` ahead of the chrome:-about rewriter; `//roamux/browser/scheme` dep edge on `//chrome/browser`. Alias map + logic live additively in `//roamux/browser/scheme` |
| `0029-mac-app-icon-roamux.patch` | **persistent** | the E0 app-icon branding (roam-103): rewires the two mac icon `bundle_data` sources in `chrome/BUILD.gn` — `chrome_app_icon` → `//roamux/app/resources/icons/mac/app.icns` and `chrome_asset_catalog` → `//roamux/app/resources/icons/mac/Assets.car` — so the built bundle carries the bold-X payloads on **both** icon channels (`CFBundleIconFile` legacy + `CFBundleIconName`/Assets.car, which is what modern macOS displays; swapping the .icns alone changes nothing visible). The same targets feed the Alerts helper app — branding it too is intended. Payloads + regen recipe live additively in `roamux/app/resources/icons/mac/`; gate: `roamux/build/check_app_icon.py`. On uprev conflict: upstream is mid-migration on mac iconography (Icon Composer content already compiled into the stock Assets.car at M149) — re-resolve by re-pointing whatever icon bundle_data targets exist at the Roamux payloads, both channels |
| `0030-sparkle-framework-rpath.patch` | **persistent** | the E6 Sparkle dyld resolution (roam-121): flag-gated (`roamux_enable_sparkle`) `-Wl,-rpath,@loader_path/../../..` on `chrome_framework`'s `!is_component_build` link, so the framework's `@rpath/Sparkle.framework/…` dependency resolves to the app's `Contents/Frameworks` in every loading process (browser + helpers). GN drops `ldflags` from source_set configs, so this cannot live in `//roamux/app` (that misdesign shipped a build that aborted at first launch — run 29232430727's artifact); gate: `roamux/build/check_framework_rpath.py` in the release pipeline |
| `0033-settings-about-roamux.patch` | **persistent** | the roam-140 branded `chrome://settings/help`, reworked by **roam-160** to drive the NATIVE update row: flag-gated (`roamuxBrandedAbout`) About-page rebrand (Roamux hero logo/title, roam-156's version lines, roam-167's 48px hero) PLUS the native-row behaviour — NEED_PERMISSION renders the offer + [Download]/[Skip], [Try again] on recoverable FAILED (suppressed for signature failures via the `roamuxUpdateErrSignature` copy comparison), widened `#buttonContainer`, the branded learn-more suppression (no support.google.com), the dimmed alpha-only raw-Sparkle line, and the Sparkle-routed Relaunch via `roamuxInstallAndRelaunch`; `about_page_browser_proxy.ts` gains the three roam-160 commands; the settings `BUILD.gn` merge is static-assets-only (`roamux_about` logo/glyph grdp; the card's ts_deps/path-mappings retired); the sparkle-gated `//roamux/browser/updates:update_service` dep + the `roamux_version_updater.{cc,h}` compiled-into sources on `//chrome/browser/ui`; and the `chrome/test/data/webui` wiring for the surviving about/menu suites. **Retired with roam-160:** the `<roamux-update-card>` embed + `Website|GitHub` links row, the `SettingsUI::BindInterface(UpdatePageHandlerFactory)` overload + `updatesAvailable` gate (patches 0032/0035/0036 deleted with the card). **roam-161** repoints the branded "Get help with Roamux" row at the repo's `docs/help.md` (ungated OpenWindowProxy import + a roamuxBrandedAbout branch in `onHelpClick_`; new-tab pending-URL browsertest). Gates: `roamux/build/check_update_row.py` + `roamux_update_row_browsertest.cc` |
| `0034-settings-menu-roamux-chip.patch` | **persistent** | the roam-140 left-nav chip: flag-gated (`roamuxBrandedAbout`) Roamux logo `img` + `$i18n{version}` sub-text on the settings-menu `#about-menu` entry (`settings_menu.{html,ts}`) **roam-157** rebuilt the chip as a two-line entry (Roamux glyph over "About Roamux" / "v <version>"), repointed the icon at roamux_glyph.svg (transparent glyph, not the tile), and added #about-menu-scoped CSS. **roam-167** made the chip version text follow the selected colour so it stays legible when selected in dark mode. |
| `0040-sparkle-version-updater.patch` | **persistent** | the roam-160 native-updater seam: (a) `VersionUpdater::Create()` (`version_updater_mac.mm`) returns the Sparkle-backed `roamux::updates::RoamuxVersionUpdater` under `ROAMUX_ENABLE_SPARKLE` when the profile has a RoamuxUpdateService (stock `VersionUpdaterMac` otherwise — OTR/system profiles and flag-off stay byte-identical); (b) `AboutHandler` (`about_handler.{cc,h}`) gains the three flag-gated consent messages `roamuxDownloadUpdate`/`roamuxSkipUpdate`/`roamuxInstallAndRelaunch` dispatching to the per-profile service (null-safe). The adapter itself is additive `//roamux/browser/updates` code compiled into `//chrome/browser/ui` via 0033's sources hunk (the 0009 precedent). Gate: `check_update_row.py` shape phase (three-file surface, flag-guarded interposition, anchored hunks) |
| `0037-current-channel-logo-roamux.patch` | **persistent** | the roam-158 toolbar-logo fix (light path; dark → roam-169): repoints `CurrentChannelLogoResourceId()`'s unbranded (`Channel::UNKNOWN`, all channels via the `CHECK_IS_TEST` fallthrough) arm at `IDR_ROAMUX_PRODUCT_LOGO_32` so every `chrome://theme/current-channel-logo` consumer (settings toolbar, `chrome://version`, the settings drawer, management/extensions banners, searchbox pedal icon) serves the Roamux glyph instead of the Chromium pinwheel. Registration hunks: the `roamux/app/resources/theme/roamux_theme_resources.grd` entry in `tools/gritsettings/resource_ids.spec` (`structures: [10140]`), the per-percent pak in `chrome_repack_percent` (`chrome_paks.gni` sources+deps), and the grit-header dep edge on `source_set("webui")`. Unconditional — branding identity, not a gated feature (the roam-103/GRIT-rebrand posture). Glyph sources + grd live additively in `roamux/app/resources/theme/`; gates: `roamux/build/check_toolbar_logo.py` (tier-1) + `roamux_theme_logo_browsertest.cc` (tier-2). Direct `IDR_PRODUCT_LOGO_32` consumers (management/extensions use the repointed helper; only non-current-channel-logo art keeps upstream branding) are deliberately untouched |
| `0038-dark-logo-roamux.patch` | **persistent** | the roam-169 dark-mode logo fix (completes roam-158's "identical in light and dark"): (Path A) replaces the content of `ui/webui/resources/images/chrome_logo_dark.svg` — the dark `<source>` every default-slot `cr-toolbar`/`cr-drawer` `<picture>` (and `contextual_tasks`' dark `<img>`) resolves, bypassing `current-channel-logo` in dark mode — with the committed Roamux glyph payload `roamux/app/resources/theme/chrome_logo_dark.svg` (roam-157 geometry, intrinsic size 24); (Path B) rewrites `about_version.html`'s desktop `<picture>` (`chrome://theme/IDR_PRODUCT_LOGO{,_WHITE}` — Chromium wordmarks in BOTH themes) to a single `current-channel-logo@1x/@2x` `<img>`, making the version body logo a roam-158-chain consumer. Unconditional — branding identity (roam-103/158 posture). Out of scope: the `theme_helper.cc` alternate-logo side path (raw IDR pair under theme packs), the Android/iOS version arm, `contextual_tasks`' light-mode `chrome_product.svg`. Gates: `roamux/build/check_dark_logo.py` (tier-1: payload structure + patch sync) + `roamux_dark_logo_browsertest.cc` (tier-2: packed payload + version DOM) |
| `0039-omnibox-chip-roamux-icon.patch` | **persistent** | the roam-159 omnibox-chip icon: swaps the `kChromeUIScheme` arm of `ChromeLocationBarModelDelegate::GetVectorIconOverride()` from the tinted `omnibox::kProductChromeRefreshIcon` to the fixed-colour `roamux::kRoamuxProductIcon` (`//roamux/browser/ui/icons`, four palette rays, PATH_COLOR_ARGB overrides the security-chip tint — identical in light/dark; the unbranded in-function `kGoogleColorIcon` precedent), plus the dep edge on `source_set("toolbar")`'s non-Android scope. Surgical by design: the upstream `.icon` and its ten-plus other consumers (app menu, six infobars, autocomplete dropdown, omnibox actions, searchbox/webui mappings, glic mac provider) keep the Chromium glyph. The WebUI-browser location bar's icon-mapping CHECK (`webui_browser_page_handler.cc`) never sees the Roamux icon: `kWebUILocationBar` is FEATURE_DISABLED_BY_DEFAULT at the pin and the roam-99 env-guard pins the set off in overlay tests — if an uprev flips it, add the mojom/app.ts/icons.html.ts mapping the CHECK names. Unconditional — branding identity (roam-103/158/169 posture). Gates: `roamux/build/check_omnibox_icon.py` (tier-1: icon structure + patch surgicality incl. context lines) + `roamux_omnibox_icon_browsertest.cc` (tier-2: chrome:// arm returns the Roamux icon; http/https get no override) |
| `0041-location-bar-roamux-scheme-display.patch` | **persistent** | the roam-179 omnibox display branding: wraps the return of `ChromeLocationBarModelDelegate::FormattedStringWithEquivalentMeaning` in `roamux::MaybeBrandFormattedUrlForDisplay` (flag-gated `kRoamuxSchemeAlias`, presentation-only — committed URLs/origins untouched; the branded text re-parses to the same destination via the roam-91 forward rewrite, which is this method's documented contract; the in-function dom-distiller transform in `LocationBarModelImpl::GetFormattedURL` is the upstream precedent one layer down), plus the include and the `//roamux/browser/scheme` dep edge on `source_set("toolbar")`. Covers `GetFormattedFullURL` AND `GetURLForDisplay` (both funnel through `GetFormattedURL`), so omnibox display and focused-edit text brand together. Helper + logic live additively in `//roamux/browser/scheme`; gates: `roamux_scheme_display_unittest.cc` (tier-1 shape) + `roamux_scheme_display_browsertest.cc` (tier-2 display/round-trip/renderer-block re-proof) |
| `0042-chrome-urls-roamux-branding.patch` | **persistent** | the roam-179 URL-directory branding: `chrome_urls_ui.cc` adds the `roamuxSchemeDisplay` loadTimeData boolean (from `kRoamuxSchemeAlias`; `//chrome/browser/ui` already deps `//roamux/common`); `chrome_urls.html` gains the `strings.m.js` script tag; `app.ts` gains the flag-guarded (`valueExists` — the shared components page also serves iOS, which never adds the boolean) `displayUrl_`/`pageTitle_` helpers; `app.html.ts` renders the `<h2>` title ("List of Roamux URLs") and the webui/internal link/row TEXT through them; command-URL rows stay UNBRANDED (plan D4 — type-only debug URLs). Link `href`s deliberately stay `chrome://` — renderers cannot request `roamux://` (the 0028 handled-scheme block, re-proven for generic hosts by roam-179's browsertests), so branded hrefs would dead-click. Flag-off serves the upstream page unchanged (modulo the inert `strings.m.js` module tag that feeds loadTimeData). Gate: `roamux_chrome_urls_branding_browsertest.cc` |
| `0043-tab-strip-position-flags-entry.patch` | **persistent** | roam-185: the `chrome://flags/#roamux-tab-strip-position` entry (`about_flags.cc` FeatureEntry via `FEATURE_VALUE_TYPE(roamux::features::kTabStripPosition)`) making the default-on E1 feature user-toggleable; `flag-metadata.json` entry (expiry -1) + both roamux flags added to `flag-never-expire-list.json` and their owners set to a valid email (0024 metadata debt cleanup) |

Each patch is **tiny, reviewed, and fails loudly on rebase**. Keep the surface minimal — it is the
rebase-cost surface tracked by §7.6/§12.5; the authoritative hook inventory is plan **§12.2**.
